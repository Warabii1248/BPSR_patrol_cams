====--------------------------------------------------------------------------------
[No.57] 2026/06/10 13:43 — pcap録画ツール cmd/pcap-record 新設（Level 2 検証基盤 Phase D）

【背景 / 原因】
  ゲームアップデートによるパケット解析層の破壊（No.23/34 型）を実機検証なしで
  検知するため、Level 2（pcap 録画・replay）基盤を導入する（docs/plan_sim_l2.md）。
  本日ゲーム側アップデート予定があり、旧バージョンのベースライン録画を
  アップデート前に取得できることが最優先のため Phase D（録画）を先行実装。

【変更内容】
  cmd/pcap-record/main.go（新規）:
    - スタンドアロン録画バイナリ。本体アプリと並行起動可（Npcap 複数ハンドル）
    - NIC 自動選択（ncap.GetActiveNetworkCards・本体と同方式）/ -iface 明示指定
    - 本番と同条件（snaplen 10MB・BPF "ip and tcp"）で pcapgo により .pcap 保存
    - -duration 指定 or Ctrl+C で graceful 終了、10 秒毎進捗ログ
  .gitignore（新規）:
    - *.pcap（チャット平文を含むためリポジトリ混入防止）

【効果】
  アップデート前後のトラフィックを .pcap として保存でき、Phase E（replay）で
  パース層の生存確認・golden 回帰テストが可能になる。

【影響範囲】
  - 新規ファイルのみ（cmd/pcap-record/main.go, .gitignore）
  - 既存コード変更なし。後方互換性: 影響なし

====--------------------------------------------------------------------------------
[No.56] 2026/06/10 12:33 — 巡回本ループのRecordPatrolMoveをSwitchGroup前へ + マージ時のロード判定フィールド引き継ぎ

【背景 / 原因】
  CLAUDE.md §4-2.1 は「RecordPatrolMove は SwitchGroup の前」だが、No.35 で
  修正されたのは runStaggerProbe のみで、巡回本ループは SwitchGroup 全完了後
  （switchDoneAt 時刻）に登録していた。これにより (1) 巡回中の再バインドが
  成立しない、(2) ExpectedCh 更新遅れによる一時的な [Patroller][Mismatch] ログが
  発生し得た。
  また mergeSessionIfDuplicate ではマージ時に postLoadFiredForLineID /
  lineIDChangedAt が existing にコピーされず、0x2E 先着パターンで
  PostLoadReady が同一 lineID で二重発火し得た。

【変更内容】
  mumu/mumu.go:
    - switchDoneAt 直後にあった「targets 全件への RecordPatrolMove」ループを削除
    - p.switchStartTimesMu.Unlock() の直後・SwitchGroup の for ループの前に、
      targets 全件・時刻 switchStartAt で RecordPatrolMove を呼ぶブロックを新設
      （runStaggerProbe 内の RecordPatrolMove は変更なし）
  ncap/cap_device.go:
    - mergeSessionIfDuplicate の existing.userUID = newSess.userUID の直後に、
      newSess.postLoadFiredForLineID != 0 なら existing にコピー、
      existing.lineIDChangedAt が newSess.lineIDChangedAt より古ければ更新する
      処理を追加

【効果】
  - per-device CH バインド予約が ADB 発行前（switchStartAt 時点）に揃うことで
    §4-2.1 の不変条件どおりの動作になり、巡回中の再バインド不成立や
    ExpectedCh 更新遅れによる一時的 Mismatch ログが解消される
  - セッションマージ後も PostLoadReady の dedup 状態（lineID単位）が維持され、
    同一 lineID での二重発火を防止する

【影響範囲】
  - mumu/mumu.go, ncap/cap_device.go
  - 後方互換性: config.json のキー・スキーマ変更なし

====--------------------------------------------------------------------------------
[No.55] 2026/06/10 12:24 — NotifyChMovePacketのUID検証 + 0x2E donor lineID補完を同一userUID限定に

【背景 / 原因】
  ネイティブクライアントの ch 移動で "[Instance-K][0x2E] UUID=...(UID=N)" ログが出ると、
  guiWriter.Write → Patroller.NotifyChMovePacket(label) が excludeUIDs 未適用・lineID=0 で
  moveSignal に phantom シグナルを注入していた（label のみで serial 解決するため、
  ネイティブと巡回対象インスタンスの label が一致/誤解決すると誤カウントの原因になる）。
  また processSyncToMeDeltaInfo 末尾の 0x2E donor lineID 補完は「同 clientIP で
  confirmedAt 最新」のセッションを donor とするため、MuMu NAT 環境では直前に ch 移動した
  別インスタンス（ネイティブ含む）のセッションが donor に選ばれ、ch を誤って借用し得た。

【変更内容】
  gui/gui.go:
    - extractInstanceLabel の直後に extractUIDFromLine ヘルパーを追加。
      "(UID=N)" 形式から UID を抽出（見つからない・解析失敗時は 0）
    - import に "strconv" を追加
    - guiWriter.Write 内の NotifyChMovePacket 呼び出しを
      NotifyChMovePacket(extractInstanceLabel(line), extractUIDFromLine(line)) に変更
    - Server.NotifyChMovePacket のシグネチャを (label string, uid uint64) に変更し透過転送
  mumu/mumu.go:
    - Patroller.NotifyChMovePacket のシグネチャを (instanceLabel string, uid uint64) に変更
    - uid != 0 の場合: excludeUIDs に含まれていれば破棄、uidToSerial でバインド済みなら
      そのserialで notifyMoveSignal、未バインドなら破棄（phantom防止）
    - uid == 0 の場合: 従来どおり label → labelToSerial 解決で notifyMoveSignal（変更なし）
  ncap/cap_device.go:
    - processSyncToMeDeltaInfo 末尾の lineID 補完: 外側条件に sess.userUID != 0 を追加し、
      donor の userUID（donorUID）を読み出して donorLine != 0 && donorUID == sess.userUID の
      場合のみ補完を実行するよう変更（lineID/mapIDコピー・ログ・onLineIDObserved発火は変更なし）

【効果】
  - ネイティブクライアントの ch 移動ログが、巡回対象として未バインドの uid の場合は
    moveSignal に注入されなくなり、巡回中の「移動失敗」「未応答」誤判定を防げる
  - 0x2E lineID 補完が同一 userUID（再起動後の同一アカウント）限定になり、
    MuMu NAT 環境で別インスタンス（ネイティブ含む）の ch を誤って借用しなくなる

【影響範囲】
  - gui/gui.go, mumu/mumu.go, ncap/cap_device.go
  - 後方互換性: config.json のキー・スキーマ変更なし。
  - 挙動変更点: NotifyChMovePacket 経由のフォールバックシグナルにおいて、
    uid が判明していて未バインド（uidToSerial 未登録）の場合は破棄されるようになった
    （従来は label をそのまま serial として moveSignal に送っていた）。
    識別（Identify）前のデバイスはこの fallback 経路ではカウントされなくなるが、
    正規経路（NotifyPostLoadReady）も元々バインド前提のため運用上の影響なし。

====--------------------------------------------------------------------------------
[No.54] 2026/06/10 12:13 — dwell下限クランプ修正 + extraWaitループのlineIDガード追加

【背景 / 原因】
  patrol_dwell_secs に 2 等の小さい値を設定しても「未設定なら最低5秒」のはずの
  クランプが全値に適用され、5秒に切り上げられていた。
  また No.41 で追加した stale lineID ガード（msg.lineID == 0 || msg.lineID == ch）が
  wait loop 4箇所のうち「発行前追加待ち」（extraWait:）ループにだけ未適用で、
  前サイクルの遅延発火シグナルが got にカウントされ得る状態だった。

【変更内容】
  mumu/mumu.go:
    - Start() 内の dwell 初期化: dwell <= 0 なら5秒、設定済み(>0)なら最低1秒にクランプするロジックへ変更
    - 巡回ループ内の dwell 毎サイクル再取得（currentCfg.DwellDuration）も同一ロジックに変更
    - extraWait: ループの case msg := <-sig: に他3ループと同一の lineID ガード
      （msg.lineID == 0 || msg.lineID == ch）と stale lineID skip の debuglog 出力を追加
  CLAUDE.md:
    - §4-2.11: 「wait loop（3箇所）」→「wait loop（4箇所: buffered drain / フェーズ1 / フェーズ2 / 発行前追加待ち）」に更新
    - §4-4.4: btn-ch-save / _setChSaveBtnDisabled の項目を削除（No.46 で該当コード削除済みのため）

【効果】
  - patrol_dwell_secs に 1〜4秒等の小さい値を設定した場合、その値に近い dwell で巡回できる
    （ADB連打防止のため最低1秒は維持）
  - extraWait ループでも前サイクルの stale lineID シグナルを誤カウントしなくなり、
    他3ループと一貫した挙動になる
  - CLAUDE.md が現行コードと一致

【影響範囲】
  - mumu/mumu.go, CLAUDE.md
  - 後方互換性: config.json のキー・スキーマ変更なし。
    patrol_dwell_secs <= 0（未設定）の挙動は従来どおり5秒で変化なし。
    1秒以上5秒未満を設定していたユーザーのみ実際の dwell 時間が短縮される（仕様どおりの修正）。

====--------------------------------------------------------------------------------
[No.53] 2026/05/25 09:02 — CLAUDE.mdのファイル責務マップを現行構成に更新

【背景 / 原因】
  CLAUDE.md の「§3 ファイル責務マップ」が旧構成のままで、
  gui/handlers.go 分離や mumu/screen.go 追加など最新の実ファイル構成とズレていた。

【変更内容】
  CLAUDE.md:
    - 主要ファイルの概算行数を現状に合わせて更新
    - mumu/screen.go、gui/handlers.go、appconfig/filter.go、global/*、location/store.go の責務行を追加
    - gui/gui.go の説明を分離後構成に合わせて更新
    - config/*.json、data/locations.json の説明を補強

【効果】
  変更時の参照ドキュメントが現行コードベースと一致し、
  触るべきファイルの判断ミスを減らせる。

【影響範囲】
  - CLAUDE.md
  - 後方互換性: 実装コード変更なし（ドキュメントのみ）

====--------------------------------------------------------------------------------
[No.52] 2026/05/24 — タップループの X/Y/間隔を config.json に保存・復元

【背景 / 原因】
  タップループの X・Y 座標と間隔(ms)は入力欄に値を入れても再起動後にリセットされていた。

【変更内容】
  appconfig/config.go:
    - Config struct に TapLoopX / TapLoopY / TapLoopIntervalMs を追加（json: tap_loop_x / tap_loop_y / tap_loop_interval_ms）
    - defaultConfig(): TapLoopIntervalMs = 1000
    - applyDefaults(): TapLoopIntervalMs < 100 なら 1000 にリセット（旧 config.json 互換）
  gui/assets/app.js:
    - tapLoopStart(): 開始成功時に cfgData を更新して saveConfig(true) で即時保存
    - loadConfig(): cfgData から tap-x / tap-y / tap-interval を復元

【効果】
  再起動後もタップループの設定値が維持される。

【影響範囲】
  - appconfig/config.go
  - gui/assets/app.js
  - 後方互換性: 旧 config.json に tap_loop_* キーなし → applyDefaults で補完。XY は 0 のまま、間隔は 1000ms がデフォルト

====--------------------------------------------------------------------------------
[No.51] 2026/05/24 — MuMu 17台目以降のデバイスがゴーストフィルタで除外される不具合修正

【背景 / 原因】
  MuMu Player 12 を 17 台以上起動した環境で、127.0.0.1:5587 以降のインスタンスが
  「有効デバイス」として認識されない問題。
  `adb devices` の生出力では TCP 側（127.0.0.1:5587〜）は全台 device 状態で出るが、
  emulator-* エントリは先頭 3 台分（emulator-5580/5582/5584）しか出ない。
  No.01 で追加したゴーストフィルタが「emulator-* ペアが無い TCP デバイスはゴースト」
  と判定するため、4台目以降（5587〜）が本物にもかかわらず除外されていた。
  MuMu 独自 ADB が台数によって emulator-* 形式を登録しないインスタンスを持つことが原因。
  adb connect は "already connected" で成功扱いになるが ListDevices でフィルタされていた。

【変更内容】
  mumu/mumu.go:
    - isMuMuADBPort(serial string) bool を追加。
      127.0.0.1 の奇数ポート 5555–5679 を MuMu Player 12 の正規ポートレンジと判定。
    - listDevicesOnce のゴーストフィルタに isMuMuADBPort による除外例外を追加。
      MuMu レンジのデバイスは emulator-* ペアが無くても保持される。
      レンジ外（例: 7555）は従来通りゴースト除外。
    - listDevicesOnce 冒頭に debug_verbose 時の adb devices 生出力ログを追加（診断用）。

【効果】
  MuMu Player 12 で 17 台以上起動している環境で全台が有効デバイスとして認識される。
  過去のゴースト除外（7555 等レンジ外ポート）は引き続き有効。

【影響範囲】
  - mumu/mumu.go（listDevicesOnce / isMuMuADBPort 追加）
  - 状態機械・NAT 識別キー・§4-2 不変条件への影響なし
  - 後方互換性: あり（デバイスが増えるだけで減らない）

====--------------------------------------------------------------------------------
[No.50] 2026/05/24 — 巡回サイクル平均表示が更新されない不具合修正

【背景 / 原因】
  No.39 でフェーズ名が move_start / loading → adb_sending / wait_0x2e / stabilizing / dwell_wait に
  リネームされた際、gui/assets/app.js の updatePatrolCycleStats 内トリガー条件が
  旧キー 'move_start' のまま残されていた。バックエンドは move_start を一切送らなくなったため
  cycleMsHistory に何も積まれず、平均が 0 のまま「--」表示固定だった。
  また moveFailed 時に同一イテレーション末尾でも adb_sending + 新 PhaseStartedAtUnixMs が
  セットされるため、単純な相変化だけでは偽サイクルが混入する問題も存在。

【変更内容】
  gui/assets/app.js:
    - patrolCycleStats に lastMoveIndex フィールド追加（初期値 -1）
    - updatePatrolCycleStats のトリガーを 'move_start' → 'adb_sending' に変更
    - current_index 変化検知を併用し moveFailed→retry の同イテ内 adb_sending 再発火を除外
    - cycleMs 下限 10s ガード追加（10000ms 以下を異常値として無視）
    - 巡回停止時に lastMoveIndex もリセット

【効果】
  ダッシュボード/巡回ステータスの「巡回サイクル」KPI（平均時間・ch/hour）が再び更新される。
  moveFailed を含む巡回でも偽サイクルが記録されない。

【影響範囲】
  - gui/assets/app.js のみ
  - Go コード・設定スキーマ変更なし
  - 後方互換性: 設定ファイル無変更

====--------------------------------------------------------------------------------
[No.49] 2026/05/24 — screen モード: shotTimeout 拡張 + fail-closed 化（誤判定修正）

【背景 / 原因】
  patrol_screen_poll_ms=500 / 8 台同時運用で:
    [PostLoadReady] serial=X mode=screen delay=2.0s
    [Screen] スクショタイムアウト serial=X → 非黒扱い      ← 約 3s で頻発
    [Screen] serial=X 初回非黒 → 即完了                    ← ロード未確認で進行
  原因 (mumu/screen.go の captureIsBlack):
    - shotTimeout = max(PollInterval * 5, 3秒) で 500ms 設定だと **3 秒固定**
    - 8 台並列 ADB screencap で 3 秒では足りずタイムアウト頻発
    - fail-open（タイムアウト時 false = 非黒扱い）で「初回非黒 → 即完了」誤判定
  → screen モードの「より確実な判定」目的を達成できていなかった。

【変更内容】
  mumu/screen.go:
    - shotTimeout を拡張: max(PollInterval * 15, 8秒)
      8 台並列 ADB screencap の詰まりを十分カバーする値
    - captureIsBlack を fail-closed 化:
        context.Canceled       → false 返却 (不変、他戦略勝利の正常離脱)
        context.DeadlineExceeded → **true 返却（黒継続扱い）**
        その他のエラー         → **true 返却（黒継続扱い）**
    - ログ文言も「→ 黒継続扱い」に変更

【効果】
  - スクショタイムアウト時は黒画面継続中とみなしてポーリングを続ける
  - 真に黒画面が消えた時点で「黒消失検知」で完了
  - ADB 完全死亡時は ScreenDetectTimeout (config 値、デフォ 20s) で
    runScreenStrategy がフォールバック発火「画面判定タイムアウト → 強制進行」

【影響範囲】
  - 変更ファイル: mumu/screen.go のみ
  - 設定キー変更なし、後方互換性 100%
  - time / either モードへの影響なし (captureIsBlack は screen 系経路でのみ使用)

====--------------------------------------------------------------------------------
[No.48] 2026/05/23 — チャット重複カット・[CHAT-*]ログカテゴリ化・検知不要発言での巡回ch削除

【背景 / 原因】
  1. チャット表示重複: dedup キーに label||client_ip を含めていたため、同一メッセージが
     接続インスタンス数ぶん (例: 6行) チャットペインに表示されていた。
  2. [CHAT-*] ログが chat chip の対象外: LOG_CATS.chat のテストが '[チャット' のみで、
     [CHAT-RAW]/[CHAT-DEDUP]/[CHAT-CLAIM]/[CHAT-BIND] が pkt/other に漏れていた。
  3. 検知不要プレイヤー (chat_report_excluded_senders) の発言は Discord 通知対象外だが、
     対象 ch の占有情報を無視するため巡回リストから削除されなかった。

【変更内容】
  gui/assets/app.js:
    - dedupeChatEvents: キーを ch|sender|message に変更、Map ベース実装に切替。
      同一メッセージは最初の 1 件を表示し、末尾に "(N台受信)" バッジを付与。
    - appendChatToPanel の isDup キーから label||client_ip を除外。
    - LOG_CATS.chat の test に l.includes('[CHAT-') を追加。
    - getChatCandidateScore に opts.ignoreExcluded フラグを追加 (発言者除外をスキップ可)。
    - isChatExcludedPatrolHit() 新設: excludedSenders に一致 & スコア閾値到達時に true。
    - appendChatToPanel: isChatExcludedPatrolHit(ev) の時 /api/patrol/remove-ch を呼出。
  gui/gui.go:
    - Server 構造体に patrolRemoveDedupMu / patrolRemoveDedup フィールド追加。
    - /api/patrol/remove-ch ルーティング登録。
  gui/handlers.go:
    - handlePatrolRemoveCh 新規追加: ch を patrolChannels から削除 + 30 分クールダウン。
      5 分間の二重削除 dedup あり。Discord 通知・検知履歴追加は行わない。

【効果】
  - チャットパネルが 1 メッセージ 1 行表示 (N台受信バッジ付き)。
  - ログタブの「チャット」chip OFF で [チャット]/[CHAT-*] 系ログが一括非表示になる。
  - 検知不要プレイヤーが有効 ch を発言した場合でも巡回リストから自動除外される。

【影響範囲】
  - gui/assets/app.js, gui/gui.go, gui/handlers.go
  - CLAUDE.md §4-4.3 の dedup キー定義を更新 (今回の変更で意図的にルール変更)
  - 後方互換性: config.json スキーマ変更なし

====--------------------------------------------------------------------------------
[No.47] 2026/05/23 18:01 — ロード完了判定に画面認識モード追加（黒画面消失検知）

【背景 / 原因】
  従来のロード完了判定は「0x2E UUID 受信 + 安定化遅延（固定 or 自動）」のみで、
  時間経過のみの待機ではゲーム挙動の揺らぎで早撃ち・遅延の取りこぼしが起きうる。
  BPSR のロード画面は中央が必ず真っ黒になるため、ADB スクショの黒画面消失検知を
  選択式で追加し、より確実なロード完了判定を可能にする。
  ※「0x2E は必ず先に来る」ことを前提に、安定化フェーズの中身を切替可能にする。

【変更内容】
  appconfig/config.go:
    - Config に 7 フィールド追加:
        PatrolLoadDetectMode string (json:"patrol_load_detect_mode")
        PatrolScreenPollMs int (json:"patrol_screen_poll_ms")
        PatrolScreenRegionX int (json:"patrol_screen_region_x")  -- 監視矩形 左上 X (px)
        PatrolScreenRegionY int (json:"patrol_screen_region_y")  -- 監視矩形 左上 Y (px)
        PatrolScreenRegionW int (json:"patrol_screen_region_w")  -- 監視矩形 幅 (px)
        PatrolScreenRegionH int (json:"patrol_screen_region_h")  -- 監視矩形 高さ (px)
        PatrolScreenBlackLuma int (json:"patrol_screen_black_luma")
        PatrolScreenBlackPixelRatio float64 (json:"patrol_screen_black_pixel_ratio")
        PatrolScreenTimeoutSecs float64 (json:"patrol_screen_timeout_secs")
    - defaultConfig: 各値の初期化（mode="time" で従来動作、矩形は 1280x720 想定の中央 600x400）
      ※ MuMu Player は負荷軽減のため 1280x720 で稼働させるのが基本前提
    - applyDefaults: 空文字/範囲外の補正（§4-3 No.31 罠回避のため string enum 採用）。
      W または H が 0 以下なら旧 config.json と判定して矩形 4 値をデフォルト値にリセット。

  mumu/screen.go (新規):
    - runAdbBinary: cmd.Output() ベース。CombinedOutput 不可（stderr で PNG 汚染）
    - captureScreenPNG: exec-out screencap -p → png.Decode
    - isRegionBlack(img, x, y, w, h, luma, pixelRatio): 指定矩形領域 (px) の暗ピクセル割合判定
        画像範囲外は自動クリップ
        *image.NRGBA / *image.RGBA を型アサーション + Pix 直接アクセス
        4 画素おきサブサンプリング、BT.601 近似輝度
        default ケースで image.At フォールバック
    - waitForBlackGone: 黒画面が消えるまでポーリング。タイムアウト/ctx.Done で false
    - runTimeStrategy / runScreenStrategy / runEitherStrategy:
        NotifyPostLoadReady から呼ばれる 3 戦略
    - 画面判定タイムアウト時は強制発火（永久待ち防止）

  mumu/mumu.go:
    - Config に 7 フィールド追加（LoadDetectMode 等）
    - Patroller に ctx context.Context フィールド追加
    - Patroller に postLoadCancels map[string]context.CancelFunc + postLoadCancelMu 追加
        同 serial の前回 goroutine を cancel して重複起動・goroutine リーク防止
    - NotifyPostLoadReady を mode ディスパッチに改修
        旧: time.Sleep → notifyMoveSignal
        新: per-serial cancel 管理 + runTimeStrategy / runScreenStrategy / runEitherStrategy
    - Start: p.ctx に巡回コンテキストを保存
    - Stop: postLoadCancels を全 cancel して画面判定 goroutine を即停止

  gui/handlers.go:
    - /api/config POST の inline struct に 7 フィールド追加
    - mumu.Config リテラルに 7 フィールド詰め替え追加（**両方への追記必須**）

  main.go:
    - 起動時の mumu.Config 組立に 7 フィールド詰め替え追加

  gui/assets/app.js:
    - CFG_FIELDS_PATROL に 7 項目追加
        patrol_load_detect_mode は type:'select'（既存 gas_target_enemy パターン）
    - 監視矩形 ドラッグ選択モーダル機能を追加（CFG_FIELDS_PATROL の region-picker-btn 型）
        openScreenshotPicker / closeScreenshotPicker / _ss* ヘルパー一式
        canvas 上で mousedown/move/up でドラッグ → image 自然座標(px) で矩形確定
        確定で cfg-patrol_screen_region_x/y/w/h に書き込み + saveConfig(false) 呼出

  gui/assets/index.html:
    - ss-overlay モーダル DOM 追加（デバイス選択、スクショ取得、canvas、座標表示、確定/キャンセル）

  gui/assets/styles.css:
    - .ss-modal / .ss-toolbar / .ss-canvas-wrap / .ss-coords / .btn-region-picker 追加

  mumu/screen.go:
    - CaptureScreenshotPNG (公開関数) 追加。GUI からスクショ PNG バイト列取得用
    - captureIsBlack の err ログを 3 分岐に細分化（誤解防止）:
        context.Canceled       → 「スクショ中断 (他戦略勝利)」（either の time 先勝時の正常終了）
        context.DeadlineExceeded → 「スクショタイムアウト → 非黒扱い」（shotTimeout 経過）
        その他                  → 「スクショ失敗 err=... → 非黒扱い」（真のエラー）
        fail-open の挙動（false 返却）は全分岐で不変、ログ表示のみ変更

  gui/handlers.go:
    - handlePatrolScreenshot 追加（GET /api/patrol/screenshot?serial=xxx）
        8 秒タイムアウト、mumu.CaptureScreenshotPNG を呼んで image/png で返す

  gui/gui.go:
    - /api/patrol/screenshot ルート登録

【効果】
  - "screen" / "either" モードで画面の黒画面消失をロード完了の根拠として利用可能に
  - "either" モードでは時間経過と画面判定のうち先勝ちで進行 → 取りこぼし耐性向上
  - 旧 config.json は patrol_load_detect_mode キー欠損 → "time" 補正で完全後方互換

【影響範囲】
  - 変更ファイル: appconfig/config.go, mumu/screen.go (新規), mumu/mumu.go,
                  gui/handlers.go, main.go, gui/assets/app.js
  - 後方互換性: 旧 config.json は "time" モードで従来動作（影響なし）
  - 不変条件: §4-2 No.6 (完了シグナル経路 = 0x2E UUID 受信 + 遅延) は維持
              （安定化フェーズの中身を切替可能にしただけ、経路自体は不変）
  - リスク: handlers.go の inline struct + mumu.Config リテラル 2 箇所書き換え漏れに注意
            （該当 commit では両方確認済み）

====--------------------------------------------------------------------------------
[No.46] 2026/05/23 — 巡回ページUI改修: 巡回chマトリクス化・GAS連携を巡回ページへ移設

【背景 / 原因】
  巡回チャンネル選択 UI が number input の動的リスト方式で操作が煩雑だった。
  GAS連携チェックボックスが設定>その他にあり、巡回機能との関連が UI 上見えづらかった。

【変更内容】
  gui/assets/styles.css:
    - .ch-sel-grid / .ch-sel-cell クラスを追加（10列グリッド・選択状態スタイル）
  gui/assets/index.html:
    - card-patrol-channels: number input リスト → 10x10 ch 選択マトリクスに変更
    - ＋追加/↑昇順/↓降順/💾保存/一括上書き入力を削除
    - 全選択/全解除ボタン・選択件数表示(ch-selected-count)を追加
    - card-patrol-control 末尾に GAS連携チェックボックス(cfg-gas_enable)を追加
  gui/assets/app.js:
    - renderChannelEditor → renderPatrolChSelector に置換（10x10マトリクス描画）
    - toggleChannel / selectAllChannels / clearAllChannels / _autoSaveChannels 追加
    - 旧 addChannel / removeChannel / sortChannels / bulkImportChannels / saveChannels / _setChSaveBtnDisabled 削除
    - CFG_FIELDS_MISC から gas_enable を削除（静的 HTML 要素に移設）
    - loadConfig に cfg-gas_enable の手動セット＋onchange リスナー追加
    - saveConfig に cfg-gas_enable の手動収集追加

【効果】
  - 巡回チャンネル選択がクリック操作で即保存に簡略化
  - GAS連携が巡回機能の文脈に集約されて発見性向上

【影響範囲】
  - gui/assets/styles.css, gui/assets/index.html, gui/assets/app.js のみ
  - バックエンド・config スキーマ・channels.txt フォーマットは無変更
  - 後方互換性: 維持（gas_enable の保存経路・デフォルト値は変更なし）

====--------------------------------------------------------------------------------
[No.45] 2026/05/23 — シグナル待ちフェーズ中のタイムアウトカウントダウン非表示

【背景 / 原因】
  wait_0x2e (シグナル待ち) フェーズでは予測残り時間 (~Xs) を label に表示しているため、
  ⏱ Xs のタイムアウトカウントダウンが重複情報となり混乱を招く。

【変更内容】
  gui/assets/app.js:
    - currentPhase === 'wait_0x2e' の時は timeoutText を空にして非表示
    - 他フェーズ (adb_sending, stabilizing, dwell_wait) は引き続き表示

【効果】
  シグナル待ち中は予測到達時間 (~Xs) のみ表示され、UI がすっきりする。

【影響範囲】
  - gui/assets/app.js のみ（Go コード変更なし）
  - 後方互換性: 影響なし

====--------------------------------------------------------------------------------
[No.44] 2026/05/23 18:00 — stale 0x2E シグナル流用による巡回完了誤判定修正

【背景 / 原因】
  Ch35 巡回中に1台（Instance-7/serial=127.0.0.1:5567）が戦闘中で操作不能になり、
  0x2E SyncToMeDeltaInfo（UUID 付き）パケットを毎秒数回連続受信し続けた。
  各パケットで NotifyPostLoadReady が無条件に発火し（dedup なし）、delay=2s 後に
  moveSignal channel へ積み続けた。金ウリボ検知で Ch35 削除 → Ch37 切替後、
  Ch37 の移動完了待ち buffered drain が stale lineID=35 のシグナル 8 個を Ch37 用に
  流用し（lineID チェックなし）、同一 serial 5567 で 8/8 を即時達成、巡回が崩壊した。

  根本3層:
  1. 0x2E ハンドラが UUID 付き毎パケットで PostLoadReady 発火（dedup なし）
  2. moveSignalMsg に lineID フィールドが存在せず wait loop で stale 識別不可
  3. wait loop 3箇所で respondedSet チェックが got++ の後にあり serial 重複カウント可

【変更内容】
  ncap/cap_device.go:
    - session 構造体に postLoadFiredForLineID uint32 追加
    - processSyncToMeDeltaInfo: PostLoadReady 発火を「lineID 変化後の初回のみ」に変更
      （sess.postLoadFiredForLineID != sess.lineID のときのみ発火し、同時に更新）

  mumu/mumu.go:
    - moveSignalMsg 構造体に lineID uint32 追加
    - notifyMoveSignal のシグネチャに lineID uint32 を追加し moveSignalMsg に詰める
    - NotifyPostLoadReady のゴルーチンで lineID を notifyMoveSignal へ伝搬
    - NotifyChMovePacket（GUI 経由）は lineID=0 で送信（スキップ対象外・後方互換）
    - buffered drain / フェーズ1 / フェーズ2 の3箇所を同一パターンで修正:
        (msg.lineID == 0 || msg.lineID == ch) && !respondedSet[msg.label] を
        got++ の前にガード。lineID 不一致時は debug ログに stale skip を記録

【効果】
  - 戦闘中などで 0x2E を連射するデバイスが新 Ch の完了判定を汚染しない
  - 同一 serial が同一サイクルで複数回カウントされない
  - PostLoadReady ログが 1 lineID = 1 回に抑制（ログ・channel 負荷軽減）

【影響範囲】
  - ncap/cap_device.go（session 構造体・0x2E ハンドラのみ）
  - mumu/mumu.go（moveSignalMsg 構造体・notifyMoveSignal・wait loop 3箇所）
  - CLAUDE.md §4-2 に不変条件 11・12 追記
  - 後方互換性: config.json 影響なし。session 内部フィールド追加のみ

====--------------------------------------------------------------------------------
[No.43] 2026/05/23 — シグナル待ちフェーズに到達予測時間を表示

【背景 / 原因】
  wait_0x2e フェーズでは「いつシグナルが届くか」が見えず、ただ待つしかなかった。
  postLoadLatencies（過去の 0x2E 到達レイテンシ履歴）をもとに予測値を表示できる。

【変更内容】
  mumu/mumu.go:
    - PatrolStatus に SwitchStartedAtUnixMs / AvgSignalLatencySecs フィールド追加
    - 巡回ループで switchStartAt を SwitchStartedAtUnixMs にセット
    - Status() で postLoadLatencies の単純平均を AvgSignalLatencySecs として返す

  gui/assets/app.js:
    - wait_0x2e フェーズかつ avg_signal_latency_secs > 0 のとき予測残り時間を計算
    - phaseLabel に '~Xs' / '~まもなく' / '(遅延)' を追加表示

【効果】
  - 履歴が蓄積されると「シグナル待ち ~20s」のように予測を表示
  - 初回実行時は履歴なしのため通常通り「シグナル待ち」のみ表示
  - タイムアウトカウントダウン（⏱）と共存し補完的な情報を提供

【影響範囲】
  - mumu/mumu.go（PatrolStatus 追加のみ、既存ロジック変更なし）
  - gui/assets/app.js（表示ロジックのみ）
  - 後方互換性: 新フィールドは省略可、旧クライアントは無視するだけ

====--------------------------------------------------------------------------------
[No.42] 2026/05/23 15:40 — 巡回開始ボタン押下時の「巡回停止」ログ誤出力・巡回ゴルーチン停滞修正

【背景 / 原因】
  巡回開始を押しても「巡回停止」だけが出て巡回開始ログが付随しないことがあった（15:26:54〜15:27:04）。
  原因は2点:
  ① Stop() が cancel==nil（巡回停止中）でも無条件に "巡回停止" をログ出力していた。
    Start() は先頭で必ず Stop() を呼ぶため、停止中に開始ボタンを押しても "巡回停止" が先に出る。
  ② getCachedIP が serialIPCacheMu を ADB 呼び出し（最大5秒）の間ずっと握ったまま。
    8台並列の resolveLabel がほぼ直列化し、ctx cancel しても mutex 待ち中のゴルーチンは
    キャンセルを受け取れないため wg.Wait() が長期ブロックする。
    結果として連打した Stop/Start が "巡回停止" だけを吐き続け、巡回が再起動しない。

【変更内容】
  mumu/mumu.go:
    - Stop(): cancel != nil の時だけ "巡回停止" をログ出力するよう条件追加
    - getCachedIP(): mutex を ADB 呼び出しから切り離し、キャッシュ参照/書き戻しのみ短いクリティカルセクションで保護

【効果】
  - 巡回停止中に開始ボタンを押しても "巡回停止" が出なくなり、直接 "巡回開始" が出る
  - Stop() 後に 8 並列の resolveLabel が同時に走り ctx キャンセルに即応するため、
    Stop/Start ボタンが詰まる現象が解消

【影響範囲】
  - mumu/mumu.go（Stop, getCachedIP のみ。§4-2 不変条件は変更なし）
  - 後方互換性: 問題なし

====--------------------------------------------------------------------------------
[No.41] 2026/05/23 — プログレスバーにタイムアウトカウントダウン表示を追加

【背景 / 原因】
  wait_0x2e フェーズ中のタイムアウトまでの残り時間がフェーズラベルに "(Xs)" として埋め込まれており
  目立たず視認性が低かった。

【変更内容】
  gui/assets/index.html:
    - `id="dash-patrol-timeout"` span を hero-row1 に追加（パーセント表示の左）
    - `id="ps-patrol-timeout"` span を progress-text に追加（パーセント表示の左）

  gui/assets/app.js:
    - phaseLabel から "(Xs)" サフィックスを除去し専用要素に分離
    - timeoutText = "⏱ Xs"（remainingSecs > 0 の時のみ表示）
    - 残り時間に応じて色変化: >20s=accent2, <=20s=warn, <=10s=danger
    - 停止時はタイムアウト要素をクリア

【効果】
  - タイムアウトまでの残り時間が独立した要素として明確に表示される
  - 10秒以下で赤・20秒以下でオレンジに色変化し緊急度が視覚的にわかる

【影響範囲】
  - gui/assets/index.html、gui/assets/app.js のみ
  - mumu.go / appconfig / パケット解析に変更なし
  - 後方互換性: 影響なし

====--------------------------------------------------------------------------------
[No.40] 2026/05/23 14:40 — 巡回タイムアウト 2段構え構造の明確化（全体=180s固定・マージ=XX秒）

【背景 / 原因】
  No.39 で 0x2E UUID 受信 + stabilization_delay をロード完了シグナルに変更したが、
  `PatrolMoveTimeoutSecs=40s`（effectiveMove）が旧シグナル（lineID-change≈8s 後）向けの値のまま
  だったため、新シグナル（0x2E≈28s + delay14s = 42s 後）がタイムアウトに 2-6 秒差で負ける。
  全サイクルが「移動失敗（完了シグナルなし）」となり、loadHistory も更新されず永遠に失敗し続ける
  無限ループになっていた。

【変更内容】
  appconfig/config.go:
    - PatrolMoveTimeoutSecs コメント更新（「全台移動失敗の判定閾値」に明確化）
    - PatrolMoveTimeoutSecs デフォルト 30 → 180（全台クラッシュ・ゲーム未起動等の安全網）

  mumu/mumu.go:
    - effectiveMove 計算を adaptiveMaxLoad なしの固定値（currentCfg.MoveTimeout）に変更。
      役割は「1台もシグナルが来なければ全台失敗」のサーフェスネットタイムアウト。
    - effectiveMerge（1台目シグナル後の残り待ち）は既存の PatrolMergeTimeoutSecs のまま維持。
    - postLoadLatencyP90() ヘルパーを追加（autoStabilizationDelay と同ロジック、×0.4 なし）。
    - dwell 延長計算を adaptiveMaxLoad(MoveTimeout) から postLoadLatencyP90() ベースに変更。
      MoveTimeout=180s になると adaptiveMaxLoad が 180s を返して dwell が 135s 延長される問題を修正。
      新式: P90(postLoadLatencies) + stabilization_delay + 5s バッファ を推定最大とする。

【効果】
  - Ch8 パトロール: 0x2E シグナルが 42s 後に届いても 180s タイムアウトが余裕で待つ → 成功
  - dwell 延長: P90=36s + stab=14.4s + 5s = 55.4s → 経過 45s → grace=10.4s（妥当）
  - タイムアウトの意味が明確化: 全体（180s）= 完全失敗判定 / マージ（15s）= 残り台数待ち

【影響範囲】
  - appconfig/config.go: PatrolMoveTimeoutSecs デフォルト変更（既存 config.json の値はそのまま優先）
  - mumu/mumu.go: effectiveMove 計算・dwell 延長・postLoadLatencyP90() 追加
  - 後方互換性: 既存 config.json に patrol_move_timeout_secs が明示されている場合はその値を使用

====--------------------------------------------------------------------------------
[No.39] 2026/05/23 12:10 — 巡回ロード完了シグナルを 0x2E UUID 受信ベースに変更・遅延自動算出・フェーズUI詳細化

【背景 / 原因】
  以前（No.27 以前）は `[Instance-N][0x2E] UUID=…` ログが毎回出力されており、これをグレップして
  `NotifyChMovePacket → moveSignal` を駆動していた。No.27 で sess.userUID を rawUUID>>16 に正規化した
  結果、0x15/0x2E の値が一致して flip-flop がなくなり、ログはセッション初回のみ出力されるように変化。
  No.36+37 で代替として lineID-change + 6s 遅延に移行したが、lineID-change は TCP セッション開設時
  （ロード 0% 付近）にしか飛ばないため、「ロード 75% 到達シグナル」である 0x2E UUID 着信とは
  タイミングが異なり精度が低い。またロード安定化遅延が固定値だったため PC 負荷・台数の変動に追従できなかった。

【変更内容】
  ncap/cap_device.go:
    - CapDevice に onPostLoadReady func(uid, lineID, t) フィールド追加。
    - SetPostLoadReadyCallback(fn) setter 追加。
    - processSyncToMeDeltaInfo: info.Uuid != nil のブロック内で、
      userUID 変化有無に関係なく uid != 0 && sess.lineID != 0 なら毎回 go onPostLoadReady() を発火。

  mumu/mumu.go:
    - Config に LoadStabilizationAuto bool 追加（true=自動）。
    - Patroller に postLoadLatencies / postLoadMu / switchStartTimes / switchStartTimesMu 追加。
    - loadStabilizationDelay(): Auto=true なら autoStabilizationDelay() を呼ぶ。
    - autoStabilizationDelay(): 直近サンプルの P90 * 0.4 を遅延とする（2s–15s クランプ、初期値 6s）。
    - recordPostLoadLatency(serial, t, window): T_0x2E - T_switchStart を蓄積。
    - NotifyPostLoadReady(uid, lineID, t): 除外UID確認 → serial解決 → ラテンシ記録 → 遅延発火。
    - MatchLineChange: 既バインド/Probe 成立分岐から loadStabilizationDelay goroutine を削除。
      ActualCh 更新のみ担当。完了シグナルは NotifyPostLoadReady 経路に統一。
    - 巡回ループ switchStartAt 記録時に switchStartTimes[serial] も更新。
    - Phase 更新: move_start → adb_sending、SwitchGroup 完了後 wait_0x2e、
      1台目 moveSignal 受信後 stabilizing（PhaseTotalSecs=loadStabilizationDelay()）。

  appconfig/config.go:
    - PatrolLoadStabilizationAuto bool (json: patrol_load_stabilization_auto) 追加。
    - defaultConfig でデフォルト true に設定。

  gui/gui.go:
    - NotifyPostLoadReady(uid, lineID, t) ラッパ追加。

  gui/handlers.go:
    - handleConfig POST の struct に PatrolLoadStabilizationAuto 追加。
    - mumu.Config マッピングに LoadStabilizationAuto 追加。

  gui/assets/app.js:
    - phaseMap を adb_sending / wait_0x2e / stabilizing / dwell_wait に更新（旧キー互換維持）。
    - 設定フォーム CFG_FIELDS_PATROL に patrol_load_stabilization_auto / secs を追加。

  main.go:
    - mumu.Config に LoadStabilizationAuto マッピング追加。
    - SetPostLoadReadyCallback で NotifyPostLoadReady を配線。

  CLAUDE.md:
    - §4-2.6 を「0x2E UUID 受信 + 遅延」に更新。
    - §4-2.10 を「MatchLineChange は notifyMoveSignal を発火しない（NotifyPostLoadReady が担当）」に更新。
    - §4-3.3 に patrol_load_stabilization_auto を追記。

【効果】
  - ロード 75% 時点（0x2E UUID 受信）を起点とすることで完了シグナルがより実態に即す。
  - 自動モードでは直近の (T_0x2E - T_switchStart) P90 × 0.4 を遅延に使うため PC 負荷・台数に自動追従。
  - フェーズバーが「ADB送信中 / 0x2E待ち / 安定化中 / 滞在待機」と詳細化され視認性が向上。

【影響範囲】
  - 変更ファイル: ncap/cap_device.go, mumu/mumu.go, appconfig/config.go, gui/gui.go,
    gui/handlers.go, gui/assets/app.js, main.go, CLAUDE.md
  - 後方互換性: PatrolLoadStabilizationAuto は Load() でデフォルト true（欠損キーは補完）。
    既存 config.json に追加キーなしでも自動モードで動作する。
  - §4-2.6 §4-2.10 は本 No.39 で更新済み。

====--------------------------------------------------------------------------------
[No.38] 2026/05/23 00:39 — 開発環境改善 第2波（changelog skill / 自動test / PRテンプレ / pre-commit hook）

【背景 / 原因】
  第1波で CLAUDE.md / hooks / subagent / gui.go 分割 / テスト基盤を整備したが、運用面で
  残っていた以下の手作業を自動化する余地が大きかった:
    - changelog エントリ書きの No.NN 採番・日時記入を手作業
    - Edit 直後はビルドのみで test 未実行 → 回帰を後工程まで気付けない
    - PR チェックリストが暗黙（CLAUDE.md §6 に頼る）
    - Claude Code を介さない手作業 git commit は何のチェックも走らない

【変更内容】
  .claude/skills/changelog.md:
    - `/changelog <summary>` slash command を新設
    - 既存最大 No.NN を grep で取得 → +1 採番 → §7 テンプレを冒頭挿入
    - 引数を一行サマリに流し込み・現在時刻 (PowerShell Get-Date) を挿入

  .claude/settings.json:
    - PostToolUse Edit|Write|MultiEdit hook の build 成功時 branch に
      `go test ./...` を続行追加（ncap/mumu/appconfig/pb/main/cmd 編集時のみ）
    - test 失敗時は出力を additionalContext に echo
    - permissions.allow に `go test ./...` を追加

  .github/pull_request_template.md:
    - §6 チェックリスト＋ subagent レビュー項目を可視化
    - changelog 関連 No.NN / 影響範囲 / 動作確認セクション

  scripts/git-hooks/pre-commit:
    - go vet → go build → go test (-race if gcc available) を順実行
    - build.ps1 と同じ GCC 候補パスを自動探索

  scripts/install-hooks.ps1:
    - `git config core.hooksPath scripts/git-hooks` で一度実行のみ

【効果】
  - changelog エントリ書きが 30秒→5秒、採番ミス防止
  - Edit 直後に test 自動走行で回帰を即検知（PostToolUse hook）
  - 手作業 commit 時も pre-commit hook が vet/build/test を強制
  - PR 作成時に §6 チェックリストが GitHub 上で表示される

【影響範囲】
  - 新規: .claude/skills/changelog.md, .github/pull_request_template.md,
    scripts/git-hooks/pre-commit, scripts/install-hooks.ps1
  - 編集: .claude/settings.json（PostToolUse hook 拡張、permissions に go test 追加）
  - ランタイム挙動: 影響なし（開発支援ファイルのみ）
  - 後方互換性: 影響なし

====--------------------------------------------------------------------------------
[No.37] 2026/05/22 23:42 — 移動完了判定の早期発火修正（No.36 副作用対応・lineID遅延方式）

【背景 / 原因】
  No.36 で MatchLineChange 既バインド分岐に notifyMoveSignal を追加したことで完了シグナルは届く
  ようになったが、新たな問題が発生した。

  0x15 SyncContainerData（onLineIDObserved の主発火元）は新 ch サーバへの TCP セッション開設直後
  （= ゲームクライアントのロード画面中）に着信する。この時点を「移動完了」とみなすと、一部端末が
  まだロード中の状態で次の switch_channel が発行され、切替を取りこぼすデバイスが発生した。

  ※初期実装では 0x2E BaseDelta playerPos（attr id==53）着信を完了シグナルにしようとしたが、
  ch 切替時はプレイヤーエンティティが保持されるためサーバが座標更新を送らず、実機で全く発火
  しないことが判明した（logs/log.txt 実測: 巡回中0回発火、全ch 移動失敗）。

【修正内容】

  lineID-change を完了シグナルに使いつつ、PatrolLoadStabilizationSecs（デフォルト 6s）だけ
  遅延させてから notifyMoveSignal を発火する方式に変更。

  (1) mumu/mumu.go
    - Config 構造体に LoadStabilizationDuration time.Duration を追加。
    - loadStabilizationDelay() ヘルパーを追加:
        Config.LoadStabilizationDuration > 0 ならその値、0 なら 6s を返す。
    - MatchLineChange の既バインド分岐: updateActualCh 後、goroutine で遅延発火。
        go func(s, fireAt){ time.Sleep(until(fireAt)); notifyMoveSignal(s, fireAt) }
    - Probe バインド成立分岐: 同様に遅延発火を追加。
    - 「発行前再確認」ブロック（MoveTimeout if-block 末尾）は維持。
        ログラベルを [pos] → [lineID+delay] に変更。

  (2) appconfig/config.go
    - PatrolLoadStabilizationSecs float64（json: patrol_load_stabilization_secs）を追加。
      デフォルト 6。0 指定時は Patroller が 6s を使用（後方互換）。

  (3) gui/gui.go / main.go
    - LoadStabilizationDuration を Config 初期化時にマッピング追加。
    - No.31 初期実装で追加した NotifyPlayerReady を削除。

  (4) ncap/cap_device.go
    - No.31 初期実装で追加した posReadyLineID/posReadyFired フィールド、onPlayerReady コール
      バック、SetOnPlayerReady setter、firePlayerReadyIfPending ヘルパーを全て削除。

【影響範囲】
  - mumu/mumu.go: Config に LoadStabilizationDuration 追加 / loadStabilizationDelay ヘルパー
    追加 / MatchLineChange 遅延発火復活 / MatchPlayerReady・NotifyPlayerReady 削除
  - appconfig/config.go: PatrolLoadStabilizationSecs 新規キー（後方互換: 0 値時は 6s）
  - gui/gui.go, main.go: 新キーのマッピング追加 / NotifyPlayerReady 削除
  - ncap/cap_device.go: playerReady 関連を全削除
  - config.json: patrol_load_stabilization_secs キーで調整可能
  - 後方互換性: 0 値時はデフォルト 6s 動作なので既存 config.json は無変更で動作

====--------------------------------------------------------------------------------
[No.36] 2026/05/22 23:42 — ch移動完了シグナル欠落修正（No.12 未完対応・恒久化）

【背景 / 原因】
  ログで 10 ch 連続「移動失敗（完了シグナルなし）→スキップ」が発生。
  各スキップ前に全 8 台分の [Patroller][Mismatch] expected=旧ch actual=新ch が出ており
  端末は実際に新 ch へ移動完了しているにもかかわらず完了判定が機能していなかった。

  現行の完了シグナル経路:
    ncap(0x2E) → log.Printf "[Instance-N][0x2E] UUID=..." ← uid != sess.userUID の初回のみ
      → guiWriter.Write が "[0x2E] UUID=" を grep
      → Patroller.NotifyChMovePacket(instanceLabel) → moveSignal チャネル

  問題: "[Instance-N][0x2E] UUID=..." は userUID 変化時（セッション初回）にしか出ない。
  巡回中の ch 移動では既存セッションに session merge されるだけで userUID は不変。
  → UUID ログが出ない → NotifyChMovePacket が呼ばれない → moveSignal 空 → タイムアウト。

  No.12 修正（changelog No.12, !HasBinding() ガード削除）は初回バインド前の欠落を修正したが
  「UUID ログ自体が出ない」ケース（バインド済み移動）は未対応のまま再発していた。

  また switchDoneAt 基準の時刻フィルタ問題:
    ADB switch_channel done がゲームサーバの新セッション開設より遅れるケースがある
    (Ch18: ゲーム側応答 21:15:08–10 vs ADB done 21:15:11–12)。
    switchDoneAt 基準では正当な完了シグナルを弾いてしまう。

【変更】
  mumu/mumu.go:
    - Patroller 構造体に labelToSerial map[string]string を追加（serialToLabel の逆引き）。
    - LoadSerialLabelMap・resolveLabel で labelToSerial を serialToLabel と同期更新。
    - notifyMoveSignal(serial, t) ヘルパーを追加:
      serial を label として moveSignal チャネルに非ブロッキング送信。
    - MatchLineChange の既バインド分岐: updateActualCh 後に notifyMoveSignal 追加。
      これがメイン修正。onLineIDObserved の正規経路から直接 moveSignal に通知。
    - MatchLineChange の Probe バインド成立分岐: updateActualCh 後に notifyMoveSignal 追加。
    - NotifyChMovePacket: instanceLabel → labelToSerial で serial を解決してから
      notifyMoveSignal 経由で送信（未解決時はそのまま送信）。
    - knownInstances・missedCounts・crashedInstances・respondedSet のキーを
      Instance label → serial に統一。ログ表示時のみ serialLabels で label に解決。
    - moveSignal の時刻フィルタ: msg.t.After(switchDoneAt) → msg.t.After(switchStartAt)
      （3 箇所: バッファ消化・Phase1・Phase2）。
      switch_channel 発行前の古い 0x2E は弾きつつ、ADB done 遅延に依らず採用可能にする。

【効果】
  バインド済み serial に対する ch 移動時、onLineIDObserved → MatchLineChange 経路で
  moveSignal が確実に発火するようになり「移動失敗（完了シグナルなし）」が解消される。
  また switchStartAt 基準により ADB 完了前に到達した正当な 0x2E も採用される。

====--------------------------------------------------------------------------------
[No.35] 2026/05/22 23:42 — デバイス認識リグレッション修正②（probe 登録タイミング）

【背景 / 原因】
  No.34 で onLineIDObserved の発火は復旧したが、依然 0/8 バインド成立のまま。
  ログで matched 0 pending probes が 8 件全て記録されていた。

  タイミング問題:
    switch_channel コマンド発行 (T+0) → サーバーが新 TCP セッション開設 (T+6s)
    → 0x15 で (uid+lineID) 観測・発火 (T+6s)   ← ここで pendingProbes は空
    → switch_channel done → RecordPatrolMove 呼び出し (T+8s)  ← probe 登録が遅い

  mumu.go の runStaggerProbe は SwitchGroup（同期ブロック ~8s）の完了後に
  RecordPatrolMove を呼んでいたため、pending probe が存在しない状態で
  MatchLineChange が走り pending probes 0件と判定されバインドされなかった。

  旧コード（Phase A 以前）では throttle なしで毎 0x15 が発火していたため、
  probe 登録後の後続発火にたまたま噛み合って動作していた。
  Phase A の throttle 強化（セッション当たり 1 回のみ発火）により破綻が顕在化。

【変更】
  mumu/mumu.go:
    - runStaggerProbe: RecordPatrolMove を SwitchGroup の前に移動。
      ADB コマンド発行前に pendingProbes を登録しておくことで、
      サーバー側の新 TCP セッションが先行しても pending probe が存在する状態を保証。

【効果】
  probe が観測より先に登録済みになり、MatchLineChange が matched 1 pending probes と
  判定してバインドが成立。識別フェーズ 8/8 バインド成立を期待。
  Phase A の誤バインド防止（excludeUIDs・時刻ガード）は引き続き有効。

====--------------------------------------------------------------------------------
[No.34] 2026/05/22 23:42 — デバイス認識リグレッション修正（No.33 の sd==nil 早期 return バグ）

【背景 / 原因】
  No.33 で processSyncContainerData の SceneData nil パスに早期 return を追加した結果、
  probe 後の実際のバインドフロー（portMap → lineID 確定 → 0x15 で charId 確定）で
  onLineIDObserved が一度も発火しなくなり、8 台すべてのバインドが成立しなくなった。

  実際のフロー:
    switch_channel → 新 TCP 接続 → portMap が lineID を確定（接続時点）
    → 0x15 (CharBaseInfo のみ、SceneData なし) で charId が確定
  この 0x15 は SceneData を持たないため、旧コードは onLineIDObserved を呼ばなかった。

【変更】
  ncap/cap_device.go:
    - processSyncContainerData: uidNewlySet フラグを導入。
      charId が 0→N に確定した場合（uidNewlySet=true）かつ sess.lineID != 0 なら
      sd==nil パスでも onLineIDObserved を発火するよう修正。
    - sd あり パス: 発火条件を (oldCh != lineID || uidNewlySet) に拡張。
      userUID 新規確定時も lineIDChangedAt を設定して発火する。

【効果】
  デバイス認識の 8/8 バインドが復旧。
  本物クライアントの同 Ch 繰り返し 0x15 は uidNewlySet=false・oldCh==lineID のため
  発火しないまま（No.33 の誤バインド防止は維持）。

====--------------------------------------------------------------------------------
[No.33] 2026/05/22 23:42 — UID 誤バインド防止（lineID 変化時のみ通知・probe 時刻フィルタ・除外 UID・手動削除）

【背景 / 原因】
  同 PC 上で MuMu エミュレータと本物 BPSR クライアントを同時起動すると、
  本物クライアントのキャラ UID が Patroller.MatchLineChange の Probe-and-Match に
  誤マッチし、エミュレータ serial に誤バインドされる問題が発生していた。
  原因:
    1. onLineIDObserved が lineID 変化なしでも毎パケット発火していた
    2. 60 秒 probe ウィンドウ × 1 件マッチ即確定で、probe 以前から同 Ch に
       いたプレイヤーも誤マッチした

【変更】
  ncap/cap_device.go:
    - session 構造体に lineIDChangedAt time.Time フィールド追加。
      0x15 で lineID が変化した時刻を記録する。
    - onLineIDObserved フィールド型を func(uid, lineID, changedAt time.Time) に変更。
    - processSyncContainerData: SceneData nil パスの再発火を削除（重複発火による誤バインド除去）。
    - processSyncContainerData: 本流パスの onLineIDObserved 発火を oldCh != lineID の
      変化時のみに限定し、lineIDChangedAt を更新してから changedAt として渡す。

  mumu/mumu.go:
    - Patroller 構造体に excludeUIDs map[uint64]bool / excludeUIDsMu sync.RWMutex 追加。
    - MatchLineChange: シグネチャを (uid, lineID, changedAt time.Time) に変更。
      - 冒頭で excludeUIDs チェック → 除外 UID はバインド候補から弾く。
      - probe 突合条件に changedAt >= probe.sentAt - 2s を追加。
        ADB コマンド発行より前に lineID が変化していた場合を拒否。
    - NotifyLineIDChange: changedAt を引数に追加し MatchLineChange に転送。
    - DeleteSerialUIDBinding(serial): serial に紐付いた UID バインドを削除し永続化。
    - SetExcludeUIDs(uids): 除外 UID リストを設定する。

  appconfig/config.go:
    - Config.ExcludeUIDs []uint64 (json:"exclude_uids,omitempty") フィールド追加。

  gui/gui.go:
    - NotifyLineIDChange: changedAt を引数に追加し Patroller に転送。
    - SetExcludeUIDs(uids): Patroller への委譲メソッド追加。
    - handleDeleteSerialUID: POST /api/serial_uid/delete でバインド削除。
    - loadDeviceMemory JS: 各エントリに「削除」ボタンを追加（uid バインドのみ表示）。
    - deleteSerialUID JS: confirm → DELETE API → リスト再ロード。
    - CFG_FIELDS_ADB に exclude_uids フィールド（csv-num 型）を追加。
    - renderConfigFields / saveConfig: csv-num 型のレンダリング・保存処理を追加。
    - handleConfig POST: ExcludeUIDs フィールドを読み込み SetExcludeUIDs に即時反映。

  main.go:
    - SetLineIDObserver コールバックのシグネチャを changedAt 引数付きに更新。
    - 起動時に guiServer.SetExcludeUIDs(cfg.ExcludeUIDs) を呼び出す。

【効果】
  - 本物クライアントが同 Ch に静止しているだけでは onLineIDObserved が発火しないため
    誤バインド対象にならない。
  - 本物クライアントが偶然 probe 中に Ch を変えても、ADB コマンド発行より前なら弾ける。
  - GUI 設定ページ「除外 UID」欄に自分のキャラ UID を登録すれば、以後の起動でも
    そのキャラ UID が Probe-and-Match に使われなくなる。
  - データ管理ページの「削除」ボタンで誤バインドされた serial_to_uid エントリを
    手動クリーンアップできる。

====--------------------------------------------------------------------------------
[No.32] 2026/05/22 23:42 — 通知エネミーOFFのとき通知音を鳴らさないよう修正

【変更】
  gui/gui.go:
    - appendDetectionHistoryLocked に silent 引数を追加。
      OnDetectHistoryOnly（通知OFFエネミー）は [DETECTION:SILENT] タグで SSE 配信。
      OnDetect（通知ONエネミー）は従来通り [DETECTION] タグで配信。
    - JS の SSE onmessage で [DETECTION:SILENT] のとき playNotifyBeep() を呼ばないよう変更。

【効果】
  設定の「通知するエネミー」でチェックOFFのエネミーが検知されてもアプリの通知音が鳴らなくなった。
  ログへの記録・検知履歴への追加は引き続き行われる。

====--------------------------------------------------------------------------------
[No.31] 2026/05/22 23:42 — 設定UIと内部状態の不整合バグ修正

【変更】
  appconfig/config.go:
    - SaveWindowState: window_state キーのみ部分更新する実装に変更。
      従来は zero-value Config を base に unmarshal してから全フィールド書き戻しを
      行っていたため、default=true な bool フィールド（gas_enable,
      patrol_adaptive_timeout, show_no_device_dialog）がキー欠損の旧 config.json で
      ウィンドウ移動のたびに暗黙的に false へ上書きされていた。
      JSON マップとして部分編集することで他フィールドを一切触らない実装に変更。

  main.go:
    - gui.New 直後に SetGASTargetEnemy(cfg.GASTargetEnemy) を追加。
      従来は起動時に SetGASEnable のみ呼ばれ SetGASTargetEnemy は設定保存時のみ
      反映されていたため、初回保存前は gasTargetEnemy="" でフィルタ無効化状態になっていた。

  cmd/chat-reporter/main.go:
    - 起動時に SetGASEnable / SetGASTargetEnemy を追加。
    - 設定保存コールバックに SetGASEnable を追加。
      従来 chat-reporter プロセスでは gasEnable が Go ゼロ値 false のままで
      UI から ON にしても反映されなかった。

【原因 / 効果】
  SaveWindowState と Load の設計が非対称（Load は defaultConfig 起点、
  SaveWindowState は zero-value Config 起点）だったため、旧 config.json で
  ウィンドウ移動後に bool デフォルト値がサイレントに上書きされ、
  UI OFF / 内部 ON のような不整合が発生していた。

====--------------------------------------------------------------------------------
[No.30] 2026/05/22 23:42 — 通知対象外エネミー検知時に検知履歴へ記録されないバグ修正

【変更】
  gui/gui.go:
    - OnDetect() をリファクタ: 履歴記録パートを appendDetectionHistoryLocked() に切り出し。
    - OnDetectHistoryOnly() を追加: 履歴記録のみ実行し、巡回ch削除・30分クールダウンは行わない。

  main.go:
    - onDetect(): 通知対象外エネミー（Enabled=false）でスキップ時に
      guiServer.OnDetectHistoryOnly(det) を呼んでから return するよう変更。

【原因 / 効果】
  main.go の通知フィルタ（allowed=false の return）が guiServer.OnDetect() より先にあったため、
  通知対象外エネミーが検知履歴 (goldBoarHistory / gold_history.json) に記録されなかった。
  修正後は Discord 通知なし・巡回ch維持のまま検知履歴にのみ記録される。

====--------------------------------------------------------------------------------
[No.29] 2026/05/22 19:00 — 巡回: 既到達chスキップ + DBGログ重複抑制

【変更】
  mumu/mumu.go:
    - Patroller.Start() 巡回ループ: チャネル分担フィルタ後に CurrentCh==目的ch のデバイスを
      switchTargets から除外し switch_channel 発行をスキップ。
      除外分は RecordPatrolMove は呼ぶ（ExpectedCh 更新のため）。
      need（移動完了待ち期待台数）も switchTargets 件数に変更。
      Phase1 タイムアウト待ちに need>0 ガード追加（全台既到達時の誤タイムアウト防止）。
    - MatchLineChange(): 既バインドログ・Probe matchedログを Vlogf → VlogfDedup に変更。
      同一メッセージを5秒抑制、値変化時は即時出力。

  debuglog/debuglog.go:
    - VlogfDedup(tag, key, minInterval, format, args) を追加。

【原因 / 効果】
  既に目的chにいるデバイスにも switch_channel を発行していたため、0x2E 完了シグナルが届かず
  「移動失敗（完了シグナルなし）→スキップ」で巡回が強制終了していた。
  また SceneData ブロードキャスト頻度に応じて同じ DBG 行が12連発するログノイズも解消。

====--------------------------------------------------------------------------------
[No.28] 2026/05/22 18:45 — デバイス認識 ch移動未起動バグ修正（Identify 強制再識別）

【変更】
  mumu/mumu.go:
    - Patroller.Identify(): runStaggerProbe 呼び出し前に対象 serial の
      serialToUID / uidToSerial エントリをクリアし snapshot 保存するよう変更。
      これにより「🔎 デバイス認識」ボタン押下時に常に全台再識別が実行される。

【原因】
  config/config.json の serial_to_uid に保存済みの UID がある状態で起動すると、
  LoadSerialUIDMap で serialToUID が全件埋まる。
  runStaggerProbe は serialToUID[ser]==0 のものだけを unbound と判定するため、
  全 serial が「既バインド」と見なされて len(unbound)<2 で即 return。
  さらに 0x2E MatchLineChange (mumu.go:1146) も alreadyBound チェックで素通りするため
  二重にスキップされ、ボタン押下後に ch移動が一切発火しなかった。
  Identify に「既存バインドを無視して強制再識別する」手段が存在しなかった。

====--------------------------------------------------------------------------------
[No.27] 2026/05/22 18:45 — デバイス認識 ch移動未起動バグ修正（sess.userUID を UID 統一）

【変更】
  ncap/cap_device.go:
    - processSyncToMeDeltaInfo (0x2E ハンドラ):
      sess.userUID = uuid（raw UUID, ~2兆）→ sess.userUID = uuid>>16（UID, 永続ユーザーID）に変更。
      ログ出力・debuglog の rawUUID 表示は維持。変数名を uid→rawUUID / uid で意図を明確化。

【原因】
  0x15 charId ハンドラは UID（小さい値）を sess.userUID に格納していたが、
  0x2E Uuid ハンドラは raw UUID（64bit全体）を格納していた。
  - Patroller の serialToUID／config.json serial_to_uid は UID 値で永続保存されている。
  - 0x2E 経由で確立されたバインドは raw UUID で保存 → 再起動後に UID 値を読み込むと
    uidToSerial[rawUUID] がヒットせず、全インスタンスが「バインド済み」でも
    「matched 0 pending probes」が連発するという不整合が生じていた。
  - No.26 で MatchLineChange の二重バインド修正 → config 永続化が安定化 →
    次回起動時に stale UID 値が読み込まれ runStaggerProbe (mumu/mumu.go:1230) の
    len(unbound)<2 早期 return が発動 → ボタン押下後に ch移動が起きなくなった。

====--------------------------------------------------------------------------------
[No.26] 2026/05/21 23:30 — Stagger Probe ロード完了待ち修正・Bind重複防止

【変更】
  mumu/mumu.go:
    - updateActualCh(): deviceStatuses が nil の場合に early return していたバグを修正。
      識別フェーズ中は deviceStatuses が未初期化のため、バインド後も CurrentCh が 0 のままとなり
      「ロード完了待ち: 0/5台」が終始 0 だった。nil の場合は make して処理継続するよう変更。
    - MatchLineChange(): WriteLock 内でバインド済み二重チェックを追加。
      onLineIDObserved が go で複数ゴルーチン起動 → 同一 uid で並走 → 30+ 回 Bind ログ出力 +
      saveFn 同時多発 → config.json を同時読み書きして "unexpected end of JSON input" が発生していた。
      WriteLoock 取得後に serialToUID[bindSerial] != 0 なら早期 return するよう変更。

【原因】
  (1) updateActualCh: deviceStatuses nil ガードが return だったため、識別フェーズ中は CurrentCh 未更新
  (2) MatchLineChange: RLock で未バインド確認後、並走ゴルーチン全員が WriteLock に進んで重複バインド

====--------------------------------------------------------------------------------
[No.25] 2026/05/21 22:30 — 複数インスタンス チャット接続割当修正（NAT 環境 claimSessionForChat）

【変更】
  - ncap/cap_device.go: claimSessionForChat() 新関数追加
    同 clientIP のセッション群から sessions マップ参照数が最小のものにチャット接続を割当、
    NAT 共有環境（MuMu 複数インスタンス全て同一IP）でインスタンスごとに分散させる
    候補条件: serverConfirmed || userUID!=0 || serverIP!="" （fast-path 確認済みも対象）
  - ncap/cap_device.go: チャット binding を findConfirmedSessionByClientIP → claimSessionForChat に変更
  - ncap/cap_device.go: [CHAT-RESOLVE] ログを削除（0x2E ハンドラからも呼ばれ大量出力していたため）
    → [CHAT-CLAIM] に置き換え（claimSessionForChat 内のみで出力）

【原因】
  MuMu 複数インスタンスは全て同一 clientIP (192.168.0.7) を使用する NAT 環境。
  (1) findConfirmedSessionByClientIP は confirmedAt 最新の1セッションのみを返すため、全チャット接続が
      1インスタンスに集約されていた。
  (2) チャット接続到着時点で UID/lineID 未受信のインスタンスは候補外となり候補が1つしかなかった
      → serverIP 確定済みセッションも候補に含めることで起動直後も全インスタンスに分散可能。

====--------------------------------------------------------------------------------
[No.24] 2026/05/21 22:39 — 複数インスタンス チャット受信問題修正（dedup・シグネチャ・ラベル対応）

【変更】
  - ncap/cap_device.go: chatNotifyFn / SetChatNotifier シグネチャに label・userUID を追加
  - ncap/cap_device.go: dedupKey を clientIP のみ→ label+clientIP に変更（同IP別インスタンスで重複排除しない）
  - ncap/cap_device.go: [CHAT-RAW][CHAT-DEDUP][CHAT-BIND][CHAT-RESOLVE] デバッグログ追加
  - gui/gui.go: ChatEvent 構造体に Label・UserUID フィールド追加（SSE JSON に含める）
  - gui/gui.go: OnChat() シグネチャ拡張・ev に Label/UserUID をセット
  - gui/gui.go JS: dedupeChatEvents() の dedup キーを label||client_ip+ch+sender+msg に修正（主因修正）
  - gui/gui.go JS: appendChatToPanel() isDup 判定を label||client_ip ベースに変更
  - gui/gui.go JS: chatMsgHtml() のシリアル表示を ev.label 優先に変更
  - gui/gui.go JS: chatMatchFilter() フィルタを ev.label 優先に変更
  - gui/gui.go JS: chatKnownLabels セット追加・refreshChatDeviceDropdown() にラベルを統合

【原因】
  dedupeChatEvents() のキーが channel+sender+message のみで clientIP/label を含まず、
  別インスタンスからの同一発言を必ず1件に潰していた。さらにサーバー側 dedupKey も
  clientIP のみで label を含まず、同 IP 環境では別インスタンスの重複除去が過剰だった。
  chatNotifyFn に label が含まれなかったため GUI 側でインスタンス識別が不可能だった。

====--------------------------------------------------------------------------------
[No.23] 2026/05/21 22:39 — 識別フェーズ バインド未確立バグ修正（0x15 SceneData なし時の onLineIDObserved 未発火）

【変更】
  - ncap/cap_device.go: processSyncContainerData において sd==nil（ゲームバージョンアップ後）の
    早期リターン前に、portMap 補完済みの sess.lineID が取れている場合は onLineIDObserved を発火する
    ようにした。これにより Stagger Probe の serial↔UID バインドが正常に確立されるようになった。
  - ncap/cap_device.go: handleServerToClientFast の serverIP 更新時に tryPortMapLineID を追加。
    fast-path 経由で新規作成されたセッションは handleClientToServer を通らないため
    tryPortMapLineID が呼ばれず sess.lineID が 0 のままだったのを修正。
  - mumu/mumu.go: runStaggerProbe の RecordPatrolMove を switchT（ループ開始時刻）→ time.Now()
    （各シリアルの切替完了時刻）に変更。probeWindow を 15s → 60s に拡大。
    5台を順番に切替すると最後のデバイスは switchT から 30s 以上後に 0x15 が到着し
    probeWindow を超過してマッチ 0 になっていた。

【原因】
  ゲームのバージョンアップで 0x15(VData) に SceneData が含まれなくなり sd==nil で即リターン。
  tryPortMapLineID は serverIP 設定時に呼ばれるが、fast-path（S→C方向）経由でセッションが
  作成される場合は handleClientToServer を通らないため tryPortMapLineID が一切呼ばれず
  sess.lineID が 0 のまま → onLineIDObserved が発火せずバインドが 0/N台のまま停止。
  加えて probeWindow=15s がシリアル切替の直列実行時間（5台×6s=30s）より短く、
  UID が正しく検出されても pendingProbes とのマッチが失敗していた。

====--------------------------------------------------------------------------------
[No.22] 2026/05/21 16:51 — データ管理ページ：記憶済みデバイス情報表示・セクション折りたたみ追加

【変更】
  - mumu/mumu.go: Patroller.GetSerialMaps() 追加（serial→uid/label マップのコピーを返す）
  - gui/gui.go: /api/devices/memory エンドポイント追加（記憶済みデバイス情報をJSON返却）
  - gui/gui.go: データ管理ページに「記憶済みデバイス情報」カード追加（serial/label/UID/ch/確認状態）
  - gui/gui.go: chマッピング管理・記憶済みデバイス情報 の両カードを折りたたみ可能に変更
  - gui/gui.go: localStorage で折りたたみ状態を永続化（dm-minimized-portmap / dm-minimized-devmem）

====--------------------------------------------------------------------------------
[No.21] 2026/05/21 16:51 — serial_to_label 自動判定・永続保存・採番リザーブ実装

【変更】
  - mumu/mumu.go: resolveLabel を3段階優先に変更
      1) 確定済みマップ（永続保存済み）
      2) Config の手動設定
      3) GetLabelByIP 動的解決 → 新規確立時に config.json へ自動書き戻し
  - mumu/mumu.go: Patroller に serialToLabel・saveSerialLabelFn 追加
      LoadSerialLabelMap / SetSaveSerialLabelFn メソッドを追加
  - mumu/mumu.go: ResolveAllIPs 追加（複数シリアルのIPを並列取得）
  - ncap/cap_device.go: 採番リザーブ機能追加（SetReservedLabels / assignLabel）
      - 起動時に保存済みラベルから IP→番号マップを構築し ncap に渡す
      - 該当IPのセッション作成時は指定番号を優先採番
      - リザーブ済み番号は releaseInstanceLabel でフリーリストに戻さない
  - main.go: 起動時セットアップ追加
      - LoadSerialLabelMap / SetSaveSerialLabelFn 設定
      - 非同期で ResolveAllIPs → SetReservedLabels を実行
      - buildIPToNumFromSerialLabel ヘルパー追加
  - gui/gui.go: LoadSerialLabelMap / SetSaveSerialLabelFn メソッドを追加
  - gui/gui.go: serial_to_label 設定欄の説明文を更新（自動判定・保存の旨を追記）

====--------------------------------------------------------------------------------
[No.20] 2026/05/21 15:36 — ダッシュボード レイアウト変更・入れ替えバグ修正

【変更】
  - チャットログを接続デバイスの直上（グリッド外）へ移動
  - 発見報告候補が右カラム全体を占有するよう拡張

【修正】
  - swapDashColumns: grid.children[1] 判定でhandleの位置ずれにより3回目以降の入れ替えが無効になるバグを修正
  - compareDocumentPosition による gold/chatCol の相対位置判定に変更
  - saveDashboardLayout も同様の修正

====--------------------------------------------------------------------------------
[No.19] 2026/05/21 22:00 — 巡回分担ロジック・デバイス認識完了判定 実装

【変更】
  E-1. デバイス認識完了判定
    - GET /api/devices/identified: シリアル別に Confirmed && LineID > 0 を確認し {identified, total, ready, missing} を返す
    - データ管理タブにデバイス認識状態バッジ追加（✅/⚠ 表示）

  E-2. 全chマッピング後の自動セット
    - 「全chマッピング」実行後、5秒ごとに /api/devices/identified をポーリング
    - 全デバイス認識完了で確認ダイアログ → OK で 1〜100ch を巡回リストに自動反映
    - 同時にデバイス分担設定も自動計算・保存

  E-3. デバイス数に応じたch分担計算
    - mumu.ComputeDeviceAssignments(deviceCount, totalCh): ペア単位で ch を等分割
      - 1台: 全範囲 / 2台: 1ペア全範囲 / 4台: 2ペア各50ch / 6台: 3ペア各33ch
      - 奇数台: floor(N/2)ペアが担当、残り1台アイドル
    - POST /api/devices/assignments/compute: 計算→ config/device_assignments.json 保存 → Patroller反映
    - 巡回ループ: channelSerials マップにより各チャンネルに割り当てられたデバイスのみ移動
    - 起動時に config/device_assignments.json があれば自動ロード

変更ファイル:
  mumu/mumu.go … GroupAssignment 型・ComputeDeviceAssignments・Save/LoadDeviceAssignments・Patroller フィルタ
  gui/gui.go … handleDevicesIdentified・handleComputeAssignments・バッジUI・ポーリングJS
  main.go … 起動時分担設定ロード
====--------------------------------------------------------------------------------
[No.18] 2026/05/21 20:00 — UI/巡回ロジック 大規模改修

【変更】
  A. 表示・ラベル整理
    - chリスト: 「(max 99)」表示を削除
    - chリスト: カード見出しを「チャンネルマトリクス」→「chリスト」にリネーム
    - ダッシュボード: 稼働時間タイル内の現在時刻表示を削除
    - サイドバー: 現在時刻表示を削除

  B. 用語統一: 「満員」→「移動失敗」
    - Go フィールド: FullChannels → MoveFailedChannels, ConsecutiveFullCount → ConsecutiveMoveFailCount
    - 設定キー: full_threshold → move_fail_threshold, consecutive_full_threshold → consecutive_move_fail_threshold
    - HTTP: /api/patrol/clear-full → /api/patrol/clear-move-failed
    - JS/HTML: clearFullChannels → clearMoveFailedChannels, 関連ID全件
    - ログ/表示テキスト: 「満員」→「移動失敗」

  C. レアエネミー検知履歴
    - monster_name を「金ウリボ」「金ナッポ」「銀ナッポ」の3種に正規化保存（起動時ロードで既存データも移行）
    - フィルタバー追加（すべて / 金ウリボ / 金ナッポ / 銀ナッポ）。ダッシュボードと巡回ページ両方
    - 検知済み★マーク: 20時間経過で自動消滅

  D. ダッシュボードレイアウト編集を有効化
    - dash-grid2の2カラム（金履歴・チャット）を「レイアウト編集」モードで左右入れ替え可能化
    - 配置を localStorage.dashLayoutV2 に保存・復元

  E. 巡回ロジック改修
    - 「指定chマッピング」削除、ch/IP/ポート手動入力欄に置き換え（/api/portmap/manual）
    - 「巡回リストへ反映」「更新」ボタン削除

  F. 稼働時間カウント: 巡回中だけ加算（累積保持）
    - pollPatrolStatus の running フラグで加算/停止を制御
    - localStorage.uptimeAccumulated に累積秒数を保存

  G. チャットログにデバイスラベル追加（デバッグ用）
    - [チャット][Instance-N ch=XX] 形式でログ出力

  H. GAS 更新停止フラグ追加
    - 設定画面「その他」に「GAS連携を有効化」チェックボックス追加
    - config.json: gas_enable フィールド追加（デフォルト: true）
    - handlePatrolChannelsGAS: gas_enable=false の場合 403 を返す

変更ファイル:
  gui/gui.go … 上記すべて（UI・JS・ハンドラ）
  mumu/mumu.go … 用語リネーム
  appconfig/config.go … 設定フィールドリネーム・GASEnable追加
  config/config.json … キー更新
  ncap/cap_device.go … チャットログにデバイスラベル追加
  main.go … SetGASEnable 配線追加
====--------------------------------------------------------------------------------
[No.17] 2026/05/21 00:00 — 巡回チャンネルリストがカードサイズに追従しないバグ修正

【バグ】巡回タブの「巡回チャンネル」カードを拡大してもチャンネルリストの高さが変わらない。
【原因】.ch-editor に max-height:180px の固定値があり、かつカード自体が flex コンテナでなかったため
        カードサイズ変更が ch-editor に伝播しなかった。
【修正】#card-patrol-channels を display:flex;flex-direction:column に変更し、
        .ch-editor の max-height:180px を削除して flex:1;min-height:0 に変更。
        カード高さに追従してスクロール領域が伸縮するようになった。

変更ファイル:
  gui/gui.go … CSS (#card-patrol-channels 追加・.ch-editor flex化)
====--------------------------------------------------------------------------------
[No.16] 2026/05/21 12:00 — 設定ページのカテゴリ分割・チェックボックス修正

【変更】設定画面を5カテゴリのカードに分割
        - Discord・通知（Webhook・チャット除外・文字数・通知エネミー）
        - 巡回・タイムアウト（滞在時間・タイムアウト・適応型）
        - ゲーム・クラッシュ復帰（パッケージ名・クラッシュ復帰）
        - ADB・デバイス（シリアルラベル・並列設定・ADBパス・座標）
        - その他（GAS対象・デバッグログ・デバイス未検出ダイアログ）
【修正】bool型フィールドのチェックボックスを [□ラベル名] 横並び表示に変更。
        従来は「ラベル名」と「□ 説明文」が縦2段で分離しており不自然だった。

変更ファイル:
  gui/gui.go … HTML構造・CFG_FIELDS分割・bool renderロジック・loadConfig更新・CSS追加
====--------------------------------------------------------------------------------
[No.15] 2026/05/21 12:00 — chマッピング更新日時がすべて起動時刻になるバグ修正

【バグ】ポートマップタブの「更新日時」列がすべて最新起動時の時刻になってしまう。
【原因】port_ch_map.json の保存フォーマットが {"ip:port": ch} だけで
        updated_at タイムスタンプを含まなかった。
        ロード時にファイル自体のModTime（≒最後にいずれかのエントリが変わった時刻）を
        全エントリに一律設定していたため、個別の更新履歴が失われていた。
【修正】JSONフォーマットを {"ip:port": {"ch": N, "updated_at": "..."}} に変更し、
        エントリごとにタイムスタンプを永続化するようにした。
        旧フォーマットは起動時に自動検出・新フォーマットへ移行して上書き保存する。
        Update()でポート変更時に古いIPのupdatedAtも削除するよう合わせて修正。

変更ファイル:
  ncap/portmap.go … JSONフォーマット変更・LoadPortMap旧フォーマット互換・save()更新
====--------------------------------------------------------------------------------
[No.14] 2026/05/21 12:00 — GUIフロントエンド機能不全箇所の修正（デッドコード削除・null安全化・エラーハンドリング）

【修正1】refreshDevices() デッドコード削除
        呼び出し箇所が無く、内部で /api/adb/restart を無条件実行する危険な関数。
        全UIは scanDevices() で代替済みのため削除。

【修正2】updateStartChDropdown の fetch エラーハンドリング追加
        .catch()が無く、サーバー未起動タイミングで unhandled promise rejection が発生。
        .catch(()=>{}) を追加してサイレント失敗させる。

【修正3】renderChannelEditor の btn-ch-save null 安全化
        document.getElementById('btn-ch-save').disabled=... を null チェックなしで呼ぶ
        箇所が複数あった。_setChSaveBtnDisabled() ヘルパーに集約し null 安全化。
        関連する removeChannel / sortChannels / bulkImportChannels / saveChannels も同様に統一。

変更ファイル:
  gui/gui.go … refreshDevices() 削除、updateStartChDropdown の catch 追加、
                _setChSaveBtnDisabled() 追加・各関数の btn-ch-save 参照を置換
====--------------------------------------------------------------------------------
[No.13] 2026/05/21 00:00 — 接続デバイスカードのch表示が自動更新されないバグ修正

【バグ】ダッシュボードの「接続デバイス」カード内のch番号が、巡回中に更新されない。
        手動で「再スキャン」を押さないと最新のchが反映されなかった。
【原因】ダッシュボードの .devpill カードの ch 表示は /api/device-map を取得した際に
        renderDashDevices() でのみ更新される。
        /api/device-map はページロード時・手動再スキャン時にしか呼ばれない。
        一方、既存の pollPatrolStatus() は2秒ごとに /api/patrol/device-statuses を
        取得して renderDeviceStatuses() を呼んでいるが、
        こちらはデバイス状態テーブル（#device-status-tbody）しか更新していなかった。
【修正】renderDashDevices() で各 devpill に data-serial 属性を追加。
        renderDeviceStatuses() の末尾に updateDashDeviceCh() の呼び出しを追加。
        updateDashDeviceCh() は DeviceStatus の actual_ch（優先）または current_ch を
        各 devpill の .dp-ch スパンに反映する。
        既存の2秒ポーリングを再利用するため追加のポーリングは不要。

変更ファイル:
  gui/gui.go … renderDashDevices: devpill に data-serial 属性追加
  gui/gui.go … renderDeviceStatuses: updateDashDeviceCh() 呼び出し追加
  gui/gui.go … updateDashDeviceCh() 関数を新規追加
====--------------------------------------------------------------------------------
[No.12] 2026/05/20 21:30 — ch移動完了シグナル欠落バグ修正（2件）

【バグ】バインド確立後、0x2E パケットが moveSignal チャンネルに届かず毎回満員判定になる
【原因】gui.go の guiWriter に `!HasBinding()` ガードがあり、バインド済みの場合は
        NotifyChMovePacket が一切呼ばれない状態になっていた。
        代替パス（onLineIDObserved → updateActualCh）は CurrentCh 更新のみで
        moveSignal へのシグナル送信は行わないため、sig チャンネルは常に空のまま
        MoveTimeout → isFull=true となっていた。
【修正】gui.go:guiWriter.Write() の NotifyChMovePacket 呼び出し条件から
        `!HasBinding()` ガードを削除。
        二重カウント防止は文字列フィルタ（"[0x2E] UUID="）と
        switchDoneAt 後の時刻フィルタで担保済みのため影響なし。

【バグ2】extractInstanceLabel がタイムスタンプ付きログ行で常に空文字列を返す
【原因】Go 標準ロガーはログ行先頭にタイムスタンプ（"2026/05/20 22:25:22 "）を付加するが、
        extractInstanceLabel は line[0]=='[' を前提とした実装になっており、
        タイムスタンプが先頭にある場合は即 "" を返していた。
        結果 NotifyChMovePacket("") → 即 return でシグナルが届かなかった。
【修正】行頭チェックを廃止し strings.Index(line, "[Instance-") でどこでも検索するよう変更。

変更ファイル:
  gui/gui.go … guiWriter.Write() の HasBinding ガード削除（1行）
  gui/gui.go … extractInstanceLabel のタイムスタンプ対応（行頭チェック→ Index 検索）
---------------------------------------------------------------------------------
====--------------------------------------------------------------------------------
[No.11] 2026/05/20 21:20 — チャット per-device 生受信ログ追加（調査用）
--------------------------------------------------------------------------------

■ 変更点

  ncap/cap_device.go:
    - tryScanChatPayload(): chat_exclude / dedup 適用前に [CHAT-RAW] ログを追加。
      出力: sess.label, clientIP, channel, hasCh, sender, message。
      デバイスごとのワルチャ受信状況を logs/log.txt で切り分け確認するための調査ログ。

====--------------------------------------------------------------------------------
[No.10] 2026/05/20 20:30 — 識別シーケンス改善・タブ自動スクロール・デバイス認識ボタン・デバッグログ
--------------------------------------------------------------------------------

■ 変更点

  mumu/mumu.go:
    - runStaggerProbe(): 識別フェーズを「ADBコマンド送信」→「全台 probeCh ロード完了待ち」に拡張。
      完了判定は DeviceStatus.CurrentCh == probeCh（0x2E受信=ロード完了）。
      タイムアウトを固定15sから cfg.MoveTimeout（最低15s）に変更。
      待機後に recordLoadTime() で初回ロード時間を記録し adaptiveMaxLoad を初期化。
    - Start() から runStaggerProbe() の自動呼び出しを削除。
      巡回開始ごとに識別フェーズが走る問題を修正。
    - Identify() メソッド追加。外部から明示的に識別フェーズを起動できる。
    - MatchLineChange(): バインド成立・既バインド更新のタイミングを [DBG] ログで詳細化。

  gui/gui.go:
    - switchView() にタブ切替後のスクロール処理を追加。
      「検知ログ」「チャットログ」タブに切り替えた際に最新行へ自動スクロール。
    - POST /api/patrol/identify エンドポイント追加（handlePatrolIdentify）。
      巡回中は 409、識別実行中は 429 を返す。識別はバックグラウンドで実行し即座に 200 を返す。
    - デバイス管理ビューに「デバイス認識」ボタン追加（runIdentify()）。
    - 設定タブに「詳細デバッグログ (debug_verbose)」トグル追加。
    - config 保存時に debuglog.Verbose をリアルタイム反映。

  appconfig/config.go:
    - Config.DebugVerbose bool フィールド追加（json:"debug_verbose"）。
      既存 config.json では未設定のため false（従来動作）のまま。

  debuglog/debuglog.go:
    - Verbose bool パッケージ変数追加。
    - Vlogf(tag, format, args...) 関数追加。Verbose=true のとき [DBG][tag] プレフィックスで出力。

  main.go:
    - 起動時に debuglog.Verbose = cfg.DebugVerbose を設定。

  ncap/cap_device.go:
    - processSyncToMeDeltaInfo(): UUID変化・lineID補完のタイミングに Vlogf 追加。

■ 注意事項

  ・デバイス認識（Stagger Probe）は巡回開始で自動実行されなくなった。
    初回起動時または再接続後は「デバイス管理」→「🔎 デバイス認識」ボタンを押すこと。
    serial_to_uid が config.json に保存済みであれば次回以降は不要。

====--------------------------------------------------------------------------------
====--------------------------------------------------------------------------------
[No.09] 2026/05/20 18:30 — パケット監視主導の per-device CH 追跡 再設計（NAT 対応・8台独立）
--------------------------------------------------------------------------------

■ 設計コンセプト

  MuMu NAT 環境では全エミュレータの clientIP がホストIPに集約されるため、
  IP ベースのデバイス識別が不可能だった。本変更では以下の3段リンクを確立する:
    ADB serial  →  userUID (CharId, パケット内プレイヤー不変ID)  →  LineId (実CH)

■ 変更点

  ncap/cap_device.go:
    - Sessions() の重複除去キーを clientIP から "uid:<userUID>" / "ep:<endpoint>" に変更。
      → NAT 環境でも各エミュレータが独立したセッションとして認識される。
    - CapDevice.onLineIDObserved フック追加。0x15/0x16 の LineId 確定時に Patroller に通知。
    - SetLineIDObserver() メソッド追加。

  mumu/mumu.go:
    - DeviceStatus に ExpectedCh / ActualCh / Mismatch / LastObservedAt フィールド追加。
    - chPendingProbe 型と Patroller の serial↔UID バインドマップ追加。
    - RecordPatrolMove(): 巡回切替直後に serial の targetCh を記録（Probe-and-Match の準備）。
    - MatchLineChange(): uid+lineID 観測時に pendingProbes と突合、候補が1件ならバインド確立。
    - NotifyLineIDChange(): 巡回中・停止中を問わず常時 CH 観測を処理。
    - updateActualCh(): ActualCh 更新・Mismatch 検出・CurrentCh への反映。
    - GetSerialUID() / GetUIDSerial() / HasBinding() アクセサ追加。
    - LoadSerialUIDMap() / SetSaveSerialUIDFn(): serial_to_uid の起動時ロードと永続化。
    - runStaggerProbe(): 未バインド serial が2台以上の場合に巡回開始前の識別フェーズ実行。
    - 巡回ループの switchDoneAt 直後に RecordPatrolMove() を全 target 分呼び出し。

  appconfig/config.go:
    - SerialToUID フィールド (serial_to_uid, omitempty) 追加。後方互換あり。

  gui/gui.go:
    - NotifyLineIDChange() 委譲メソッド追加（main.go から呼び出し）。
    - LoadSerialUIDMap() / SetSaveSerialUIDFn() 委譲メソッド追加。
    - handleDeviceMap: ipToSess に加え uidToSess (uid→session) を追加。
      serial→UID バインドを最優先で参照し、IP照合は非 NAT 環境のフォールバックに降格。
    - handleDeviceMap: DeviceStatus.ActualCh を最優先で CurrentCh に反映。
      偽フォールバック（巡回全体CHを未解決デバイスに上書き）を削除。
    - LogWriter の [0x2E] grep → NotifyChMovePacket パスに HasBinding() ガードを追加。
      バインド確立後は新パス（SetLineIDObserver 経由）のみが有効になる。

  main.go:
    - capDevice.SetLineIDObserver() で Patroller.NotifyLineIDChange() に配線。
    - cfg.SerialToUID の起動時ロードと appconfig.Save() による書き戻しを設定。
============================================================================
BPSR Patrol Cams — 変更履歴
================================================================================

--------------------------------------------------------------------------------
[No.08] 2026/05/20 07:15 — デバイス監視: タイムアウト誤表示・カラム空欄の修正
--------------------------------------------------------------------------------

■ extractUID → extractInstanceLabel (gui/gui.go)

  問題:
    - [0x2E] パケット受信ログ "[Instance-7][0x2E] UUID=... (UID=9876543)" から
      UID 数値文字列 "9876543" を抽出して NotifyChMovePacket に渡していた。
    - 一方 serialLabels[serial] は resolveLabel() で "Instance-7" 形式を返す。
    - respondedSet のキーが "9876543" で serialLabels の値が "Instance-7" のため、
      常にミスマッチ → responded=false → 全デバイスが常にタイムアウト表示。

  修正:
    - extractUID を extractInstanceLabel に置き換え。
      ログ行先頭の "[Instance-N]" プレフィックスからラベルを抽出するよう変更。
    - これにより respondedSet と serialLabels のキーが統一される。

■ ラベル解決不能時のフォールバック (mumu/mumu.go)

  問題:
    - serial_to_label 未設定 ＋ MuMu NAT 環境では resolveLabel が "" を返す。
    - label="" のとき responded := !isFull && label != "" && ... で常に false。
    - 全デバイスが ch・ロード時間・ラベル未表示でタイムアウト表示になっていた。

  修正:
    - responded := !isFull && (label == "" || respondedSet[label]) に変更。
    - label="" （ラベル照合不能）の場合、巡回成功（!isFull）ならば responded と見なす。
    - CurrentCh・LastLoadSecs が正常に更新され、状態が「正常」と表示される。

--------------------------------------------------------------------------------
[No.07] 2026/05/20 00:30 — ダッシュボード接続デバイスに現在Ch表示
--------------------------------------------------------------------------------

■ per-device 現在Ch表示 (mumu/mumu.go, gui/gui.go)

  背景:
    - MuMu Playerの全エミュレータは Windows ホストNAT経由で通信するため、
      パケットキャプチャ側の clientIP が全て同一ホストIPに集約される。
      このためIPベースでの per-device セッション照合は原理的に不可能。
    - DeviceStatus.CurrentCh（ADB serial 単位）はすでに存在していたが、
      handleDeviceMap で未使用だった。

  変更:
    - mumu/mumu.go: Patroller.SetDeviceCh(serial, ch) を追加。
      手動切替時に DeviceStatus.CurrentCh を即時更新するための exported メソッド。
    - gui/gui.go handleDeviceMap: wg.Wait() 後に GetDeviceStatuses() で
      serial→CurrentCh マップを作成し、DeviceEntry.CurrentCh に最優先でマージ。
      デバイス別データが無い場合のみ巡回全体 CurrentChannel をフォールバック利用。
    - gui/gui.go handleSwitch: 切替成功したシリアルに SetDeviceCh を呼び、
      次回 device-map 取得で即座に新Chが返るよう対応。

--------------------------------------------------------------------------------
[No.06] 2026/05/19 23:58 — 検知履歴×で巡回リスト復元
--------------------------------------------------------------------------------

■ 誤報取消: 履歴×クリックで巡回リストへ復元 (gui/gui.go)

  変更:
    - handleDeleteGoldHistory で履歴削除時に以下を追加実行:
      1. cooldownChs から対象chを削除（30分クールダウンを解除）
      2. patrolChannels に対象chを復元（未登録の場合のみ末尾追加）
      3. channels.txt を保存してPatrollerのホットリロードへ反映
    - チャット検知・パケット検知どちらの誤報にも対応。
    - 既にpatrols listに存在するchは重複追加しない。

--------------------------------------------------------------------------------
[No.05] 2026/05/19 19:50 — チャット全デバイス取得・重複排除修正
--------------------------------------------------------------------------------

■ ポート5003チャット接続修正 (ncap/cap_device.go)

  修正:
    - ゲームセッション確定前にポート5003接続が到着した場合、接続を破棄していた。
    - 確定セッションが存在すればそれを使用し、未存在の場合は即座に仮セッションを作成して
      チャット処理を継続するよう変更（ゲームログインタイミングに依存しない）。
    - 未確定セッション(serverConfirmed=false,userUID=0)でもport 5003はブロックせず
      そのまま reassembleTcpStream まで通過させる。

■ チャット重複排除の修正 (ncap/cap_device.go, gui/gui.go)

  変更:
    - バックエンドのdedupKey: clientIP+channel+sender+message のまま維持。
      各デバイスの同一メッセージは独立して通過し、フロントエンドに届く。
    - フロントエンドの appendChatToPanel isDupチェックを
      コンテンツのみ → client_ip+コンテンツ に変更。
      同一デバイスの重複は引き続きブロック、異なるデバイスからの同じ内容は別イベントとして保持。
    - renderChatPanel の dedupeChatEvents でコンテンツdedup、デバイスフィルタで
      各デバイス別にチャットを表示できるようになった。

--------------------------------------------------------------------------------
[No.04] 2026/05/19 19:30 — デバイス監視・クラッシュ検知・自動復帰
--------------------------------------------------------------------------------

■ デバイス別監視ステータス (mumu/mumu.go, gui/gui.go)

  追加:
    - DeviceStatus 構造体: シリアル・ラベル・現在ch・最終ロード時間・タイムアウト回数
      ・ADB失敗・クラッシュ・復帰中フラグを保持。
    - Patroller.GetDeviceStatuses() — 全デバイスの状態を一覧取得。
    - 巡回ループ内でシリアルごとに updateDeviceStatus() を呼び出し、
      各サイクルの結果（応答有無・ロード時間・ADB失敗）を記録。

■ ADBシリアル ↔ インスタンスラベル解決 (mumu/mumu.go, ncap/cap_device.go)

  追加:
    - CapDevice.GetLabelByClientIP(ip) — デバイスIPからインスタンスラベルを取得。
    - Patroller.resolveLabel(ctx, serial, cfg) — シリアル→IP→ラベルの2段解決。
      手動マップ（SerialToLabel 設定）優先、自動検出にフォールバック。
    - Patroller.getCachedIP(ctx, serial, cfg) — GetDeviceIP の5分TTLキャッシュ。
      巡回ループ内の過剰なADB呼び出しを抑制。
    - 巡回開始時にターゲット一覧のIPを並列解決して serialLabels マップを構築。
    - GetLabelByIP コールバックを main.go・gui.go(ホットリロード) から注入。

■ クラッシュ検知・自動復帰 (mumu/mumu.go)

  追加:
    - Patroller.checkAndRecover(ctx, serial, cfg):
      - `adb shell pidof <package>` でゲームプロセスの存在確認。
      - プロセス消失 = GameCrashed フラグを立て、CrashRecoveryEnabled 時は
        `am force-stop` → 2s待機 → `am start -n <activity>` (または monkey) で再起動。
      - CrashRecoveryDelaySecs 秒待機後にクラッシュフラグをリセット。
    - Patroller.RecoverDevice(ctx, serial) — GUIから手動トリガー可能。
    - クラッシュ確認は `!isFull && !responded && !adbFailed` の場合のみ実行。
      満員チャンネルや一時ADB失敗では起動しない。

■ 新API・GUI (gui/gui.go)

  追加:
    - GET  /api/patrol/device-statuses — 全デバイスの DeviceStatus 一覧を返す。
    - POST /api/patrol/recover — serial 指定でゲーム再起動を手動トリガー。
    - 巡回パネルに「デバイス監視」カードを追加:
        シリアル / ラベル / 現在ch / ロード時間 / 状態バッジ / 手動復帰ボタン
      2秒ごとの pollPatrolStatus に連動して自動更新。
    - 状態バッジ: 🟢 正常 / 🟡 タイムアウト / 🔴 クラッシュ / 🔄 復帰中 / ⚠ ADB失敗

■ 新設定項目 (appconfig/config.go, gui/gui.go)

    - game_package_name (string): クラッシュ検知・ADB起動用パッケージ名
    - game_launch_activity (string): 復帰時の起動アクティビティ（空でmonkey使用）
    - crash_recovery_enabled (bool, デフォルト: false): 自動復帰の有効/無効
    - crash_recovery_delay_secs (float64, デフォルト: 30): 復帰後の待機秒数
    - serial_to_label (map[string]string): シリアル→ラベルの手動バインド

--------------------------------------------------------------------------------
[No.03] 2026/05/19 18:30 — 適応型タイムアウト・ロード中デバイス早期切替防止
--------------------------------------------------------------------------------

■ 適応型タイムアウト (mumu/mumu.go, appconfig/config.go, main.go, gui/gui.go)

  問題:
    - PCの負荷によりゲームロードが長引くと MoveTimeout 内に [0x2E] が届かず
      満員と誤判定されチャンネルをスキップしていた。
    - タイムアウト後の dwell 中も他デバイスがロード中の場合に次チャンネルへの
      切替コマンドが送られ ADB操作が失敗していた。

  変更:
    - Patroller が直近N回（デフォルト10回）の全台ロード完了時間を記録。
    - 実効 MoveTimeout / MergeTimeout = max(記録最大値×1.5, 設定値) として自動延長。
      設定値はタイムアウトの下限値として機能する。
    - タイムアウト発火時でも「推定最大ロード時間 − 経過時間」を dwell に加算する
      ロード猶予を導入。ロード中デバイスが次の切替コマンドを受け取らないよう保護。
    - 新設定項目: patrol_adaptive_timeout (bool, デフォルト: true)
                  patrol_adaptive_timeout_window (int, デフォルト: 10)
    - GUI設定パネルに上記2項目を追加。

--------------------------------------------------------------------------------
[No.02] 2026/05/19 00:00 — ADB操作改善・GUI整理・チャット重複排除修正
--------------------------------------------------------------------------------

■ 起動時ADB初期化を非同期化 (main.go)

  変更:
    - 起動時の mumu.EnsureServer 呼び出しを goroutine で非同期化。
    - 以前はADB初期化完了まで GUI サーバー起動がブロックされていた。

■ チャット重複排除キーの修正 (ncap/cap_device.go)

  問題:
    - 複数デバイスが同一メッセージを受信した場合、1台分しか通知されなかった。
  変更:
    - 重複排除キーを `channel+sender+message` から
      `clientIP+channel+sender+message` に変更。
    - 同一クライアントの10秒以内の再受信のみスキップ。
      異なるデバイスからの同一メッセージは各々通知される。

■ ADB操作エンドポイント追加 (gui/gui.go)

  追加:
    - POST /api/adb/kill-server — ADBサーバーを停止する。
    - POST /api/adb/connect     — 指定シリアル（host:port形式）のデバイスを
      追加接続する。コマンドインジェクション防止のため特殊文字を拒否。

■ デバイス管理 GUI 整理 (gui/gui.go)

  変更:
    - 未使用の Server.tapper フィールドおよび mumu.NewTapper() 呼び出しを削除。
    - 連続タップカードをデバイス管理画面から削除。
    - 「再スキャン / ADB再起動」ボタンを「再スキャン」（scanDevices）と
      ADB再起動に分離。scanDevices はデバイス検出のみ行いADB再起動しない。
    - デバイス管理タブ切り替え時の loadTapConfig() 呼び出しを削除。

■ ポートマップ UI 改善 (gui/gui.go)

  変更:
    - ポートマップモーダルをスクロール可能に変更（overflow-y: auto）。
      エントリが多い場合にモーダルが画面外にはみ出る問題を修正。
    - 「📡 巡回リストへ反映」ボタンを追加（portmapSetAsPatrol）。
      ポートマップの有効ch（1〜100）を現在の巡回リストに一括反映できる。

--------------------------------------------------------------------------------
[No.01] 2026/05/18 16:05 — ADB安定化・タップループ・GUIデザイン刷新
--------------------------------------------------------------------------------

■ 巡回パネルに「全チャンネル1巡」機能を追加 (gui/gui.go)

  変更:
    - 巡回制御カードに「開始ch」「終了ch」の入力欄を追加。
    - 「⏵ 全Ch1巡」ボタンを追加し、指定範囲のchを1回だけ巡回できるようにした。
    - 実行時は開始ch〜終了chの連番リストを生成し、
      `/api/patrol/start` に `loop_mode: false` で送信する。
    - 開始ch > 終了ch の不正入力はGUI側でエラー表示して開始を抑止する。

■ デバイス一覧 — 全選択 / 全解除ボタン追加 (gui/gui.go)

  変更:
    - デバイスタブの一覧上部に「☑ 全選択」「☐ 全解除」ボタンを追加。
    - 全選択: 表示中のすべてのデバイスを一括で選択状態にする。
    - 全解除: すべてのデバイスの選択を一括で解除する。
    - デバイスリストの描画ロジックを renderDeviceList() に集約し、
      refreshDevices() と定期更新パスで共用するようにリファクタリング。
    - currentDevices / currentDeviceMap をモジュール変数として保持し、
      ボタン押下時にも最新のデバイス情報を参照できるようにした。

■ ADB デバイス検出の安定化 (mumu/mumu.go)

  問題:
    - 起動直後やADBサーバー再起動後にデバイスが認識されないことがあった。
    - 7555 番ポートの非MuMuサービスが "127.0.0.1:7555" としてゴーストデバイスに
      なり、存在しないデバイスに対してADB操作が行われていた。
    - `mumu_serials` に設定されていたポート（5581–5595）が実際の
      MuMuインスタンスと一致していなかった。

  変更:
    1. ConnectConfiguredSerials（旧: connectConfiguredSerials）
       - 外部パッケージから呼び出せるようエクスポート化。
       - gui.go の起動時チェックや /api/devices でも再利用するため。

    2. discoverLocalEmulatorADBTargets — ポートスキャン範囲の変更
       - 変更前: 5554–5610 step=2 (偶数のみ) + 7554–7570 + 16384–17376
       - 変更後: 5554–5680 step=1 (偶数・奇数両方、127ポート)
       - 理由: MuMu Player 12 は奇数ポート（5555, 5557…）を使用するため
         偶数のみのスキャンでは検出できなかった。7xxx・16xxx 範囲は
         MuMu Player 12 の実ポートとは無関係であり、7555 のゴースト原因に
         なっていたため除去した。
       - localhost の閉ポートは ECONNREFUSED で即時返るため遅延は最小限。

    3. EnsureServer — 起動後の待機を改善
       - 変更前: start-server 後に固定 500ms sleep
       - 変更後: ConnectConfiguredSerials → waitForDevices(5秒ポーリング)
       - 理由: 500ms では start-server 完了直後にデバイスが未登録の場合が
         あり、デバイス0台のまま処理が進んでいた。

    4. RestartServer — 再起動後の確認を強化
       - 変更後: ConnectConfiguredSerials → waitForDevices(15秒) を実行し、
         15秒以内にデバイスが検出できない場合はエラーを返す。
       - 理由: MuMu独自ADBはkill-server後に全インスタンスの再登録まで
         数秒かかるため、十分な待機が必要。

    5. RecoverServer（新規追加）
       - 非破壊ADB復旧: start-server + adb reconnect + waitForDevices(20秒)
       - 改善しない場合のみ RestartServer にフォールバック。
       - 理由: 不要なkill-serverはADB接続をリセットし既存セッションを
         破棄するため、まず非破壊手順を優先。

    6. waitForDevices（新規追加）
       - 1台以上のデバイスが検出されるまで指定時間ポーリングするヘルパー。
       - EnsureServer / RestartServer / RecoverServer / 巡回ループで共用。

    7. listDevicesOnce — ゴーストデバイスフィルタ追加
       - emulator-* エントリが存在する場合、対応する emulator-* を持たない
         TCP接続（127.0.0.1:XXXX）をゴーストとして除外するフィルタを追加。
       - 理由: ADBサーバーが前セッションの "adb connect" 結果を記憶しており、
         MuMu以外のサービスが開いているポート（例: 7555）への接続が
         残り続けていた。emulator-* との対応確認によって実エミュレーターのみを
         認識するようにした。

  config/config.json:
    - mumu_serials: 誤ったポート一覧（5581–5595）を空配列に変更。
      自動検出（discoverLocalEmulatorADBTargets）を使用するようになり、
      環境によってポートが異なる場合も対応可能。

■ GUI起動時チェックの改善 (gui/gui.go)

  変更:
    1. 起動時デバイス確認（巡回モード時）
       - 各リトライ前に ConnectConfiguredSerials を呼び出すように変更。
       - 3回全失敗時は RecoverServer を実行するフォールバックを追加。
       - 理由: 以前は単に ListDevices を呼ぶだけで、接続試行をしていなかった。

    2. /api/devices（handleDevices）— 自動再接続
       - デバイスリストが空で返った場合、ConnectConfiguredSerials を実行して
         再度 ListDevices を呼ぶリトライを追加。
       - 理由: GUIが開いている状態でエミュレーターを起動した際に
         手動でADB再起動しなくても自動検出されるようにするため。

■ タップループ機能追加 (mumu/mumu.go + gui/gui.go)

  追加:
    - TapLooper 構造体（mumu.go）: 指定座標への定期タップをADB経由でループ。
      複数シリアルに並列実行。Start/Stop/Status API。最小インターバル 100ms。
    - GUI APIエンドポイント（gui.go）:
        POST /api/adb/tap-loop/start   — ループ開始（tap_x, tap_y, interval_ms, serials）
        POST /api/adb/tap-loop/stop    — ループ停止
        GET  /api/adb/tap-loop/status  — 現在状態（running, tick_count 等）

■ GUIデザイン刷新 (gui/gui.go — indexHTML)

  変更:
    1. テーマシステム追加（CSS data-theme 属性）
       - Dark Blue（デフォルト）: GitHub-inspired ネイビー + 青アクセント
       - Black: OLED ピュアブラック + 電気シアン・高コントラスト
       - Light: GitHub Light スタイル
       - Gold + Black: ラグジュアリーダーク + ゴールドアクセント

    2. フォントサイズスケール追加（CSS data-fs 属性）
       - S / M / XL（デフォルト）/ 2X / 3X の5段階

    3. サイドバーにブランドヘッダー追加
       - アプリ名・バージョン表示、稼働状態ドットインジケーター

    4. CSS変数・カラートークン全面刷新
       - 全カラーを CSS変数化し、テーマ切り替えを単一属性変更で実現。
       - ハードコードされた rgba() カラー値をトークン参照に統一。

    5. 細部ブラッシュアップ
       - プログレスバー細型化（10px → 4px）
       - タイトルバー細型化（6px → 3px）
       - ナビアイテムのアクティブ状態をアクセントカラーで明確化
       - スクロールバー・フォームコントロールのスタイル統一

================================================================================
